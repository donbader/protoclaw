# Claude Code Instructions

This project uses hierarchical AGENTS.md files for context. Before working in any directory, check for an AGENTS.md in that directory and read it. Walk up to parent directories if you need broader context.

Start with `./AGENTS.md` for project-wide conventions, anti-patterns, and structure. Then load the AGENTS.md closest to the files you're changing for domain-specific rules.
