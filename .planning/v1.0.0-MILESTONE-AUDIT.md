---
milestone: v1.0.0
audited: 2026-04-16
status: passed
scores:
  requirements: 15/15
  phases: 5/5
  integration: 5/5
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 08-defaults-consolidation
    items:
      - "Serde default fns kept as fallback (not removed) — drift test guards against divergence"
  - phase: 10-ci-ide-validation
    items:
      - "Unknown key detection is top-level only — nested unknown keys not checked"
  - phase: 11-per-extension-defaults
    items:
      - "Docker workspace agents skipped for sidecar defaults (no host filesystem path)"
      - "Extension defaults only merge into options map — structural fields not overridable"
---

# Milestone Audit: v1.0.0 Config-Driven Architecture

**Audited:** 2026-04-16
**Status:** ✅ PASSED
**Phases:** 7-11 (5 phases, 8 plans)

## Requirements Coverage

All 15 requirements satisfied with test evidence.

| Requirement | Phase | Status | Evidence |
|-------------|-------|--------|----------|
| DFLT-01 | 8 | ✅ | defaults.yaml expanded, `when_config_has_no_supervisor_section_then_uses_defaults` test |
| DFLT-02 | 8 | ✅ | `when_defaults_yaml_values_match_serde_default_fns_then_no_drift` test |
| DFLT-03 | 8 | ✅ | `when_generate_config_yaml_called_then_contains_agents_and_channels_but_not_supervisor` test |
| DFLT-04 | 8 | ✅ | `when_defaults_yaml_deserialized_alone_then_all_fixed_path_fields_populated` test |
| SCHM-01 | 7 | ✅ | 4 aliases removed, `when_manager_keys_are_hyphenated_then_fields_are_silently_ignored` test |
| SCHM-02 | 10 | ✅ | `check_unknown_keys()` + `--strict` flag, 4 tests + 2 CLI tests |
| JSCH-01 | 9 | ✅ | `derive(JsonSchema)` on 19 types, 8 schema structure tests |
| JSCH-02 | 9 | ✅ | Manual impls for StringOrArray + PullPolicy, `when_schema_generated_then_*` tests |
| JSCH-03 | 9 | ✅ | `anyclaw.schema.json` committed, drift test guards it |
| JSCH-04 | 9 | ✅ | `Commands::Schema` in main.rs, prints JSON Schema to stdout |
| JSCH-05 | 9 | ✅ | `validate_schema()` in validate.rs, called before binary checks |
| CI-01 | 9 | ✅ | `when_committed_schema_file_exists_then_it_matches_generate_schema_output` runs in CI |
| CI-02 | 9 | ✅ | `when_example_configs_loaded_then_they_validate_against_schema` runs in CI |
| IDE-01 | 10 | ✅ | `when_generate_config_yaml_called_then_first_line_is_yaml_language_server_modeline` test |
| EXT-01 | 11 | ✅ | `load_extension_defaults()` + 9 unit tests, wired in Supervisor::new() |

## Cross-Phase Integration

| Wiring | Status | Detail |
|--------|--------|--------|
| Phase 7 → 9 | ✅ | Alias removal reflected in schema (no stale aliases in properties) |
| Phase 8 → 9 | ✅ | Expanded defaults.yaml values appear in schema default annotations |
| Phase 9 → 10 | ✅ | `check_unknown_keys()` calls `generate_schema()` for known-key set |
| Phase 8 → 11 | ✅ | Extension defaults merge after Figment loads base defaults |
| Phase 11 → Supervisor | ✅ | `resolve_all_binary_paths()` → `load_extension_defaults()` ordering correct |

## E2E Flows

| Flow | Status | Detail |
|------|--------|--------|
| `anyclaw init` | ✅ | Modeline + clean config (no supervisor) + valid YAML |
| `anyclaw validate` | ✅ | Schema check → unknown keys → path resolution → binary checks |
| Extension sidecar | ✅ | Sidecar discovered → options merged → user wins → managers boot |

## Integration Gap Found and Fixed

**`anyclaw validate` was not resolving `@built-in/` paths before binary checks.** Fixed in commit `81293cc` — added `resolve_all_binary_paths()` call before `validate_config()`.

## Tech Debt

| Phase | Item | Severity |
|-------|------|----------|
| 8 | Serde default fns kept as fallback (drift test guards) | Low |
| 10 | Unknown key detection top-level only | Low |
| 11 | Docker workspace agents skipped for sidecar defaults | Low |
| 11 | Extension defaults only merge into options map | Low |
