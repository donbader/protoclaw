---
phase: 05-test-coverage-verification
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - .planning/phases/05-test-coverage-verification/coverage-report.txt
autonomous: true
requirements:
  - BUGF-02

must_haves:
  truths:
    - "cargo-llvm-cov runs successfully across the workspace"
    - "Coverage percentage is compared against 75.17% Phase 1 baseline"
    - "Coverage report is saved for reference"
    - "Any code smells found during coverage analysis are documented"
  artifacts:
    - path: ".planning/phases/05-test-coverage-verification/coverage-report.txt"
      provides: "Coverage measurement results and baseline comparison"
      contains: "coverage"
  key_links:
    - from: ".planning/phases/05-test-coverage-verification/coverage-report.txt"
      to: "Phase 1 baseline"
      via: "percentage comparison"
      pattern: "75.17"
---

<objective>
Run coverage measurement and compare against Phase 1 baseline.

Purpose: D-05 requires measuring coverage after all tests are added and comparing against the 75.17% baseline from Phase 1. BUGF-02 requires documenting any code smells found during the coverage analysis pass.
Output: Coverage report showing improvement over baseline.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-test-coverage-verification/05-CONTEXT.md
@.planning/phases/05-test-coverage-verification/05-01-SUMMARY.md
@.planning/phases/05-test-coverage-verification/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run coverage measurement and compare against Phase 1 baseline</name>
  <files>
    .planning/phases/05-test-coverage-verification/coverage-report.txt
  </files>
  <read_first>
    .planning/phases/05-test-coverage-verification/05-01-SUMMARY.md
    .planning/phases/05-test-coverage-verification/05-02-SUMMARY.md
    AGENTS.md
  </read_first>
  <action>
    **Step 1: Run full workspace tests to confirm everything passes.**
    ```bash
    cargo test --workspace
    ```
    If any tests fail, investigate and fix before proceeding. Document any fixes as BUGF-02 items.

    **Step 2: Run cargo-llvm-cov for coverage measurement.**
    ```bash
    cargo llvm-cov --workspace --text
    ```
    This produces a text coverage report showing per-file and aggregate coverage percentages.

    If `cargo-llvm-cov` is not installed, install it:
    ```bash
    cargo install cargo-llvm-cov
    ```

    **Step 3: Compare against Phase 1 baseline.**
    The Phase 1 baseline was 75.17% (from ROADMAP Phase 1 / TOOL-05).
    Extract the aggregate coverage percentage from the report.
    Calculate the delta: `new_coverage - 75.17`.

    **Step 4: Save coverage report.**
    Write the coverage output plus baseline comparison to `.planning/phases/05-test-coverage-verification/coverage-report.txt`:
    ```
    Phase 5: Test Coverage & Verification — Coverage Report
    Date: [current date]

    Phase 1 Baseline: 75.17%
    Phase 5 Result: [X.XX]%
    Delta: [+X.XX]%

    [Full cargo llvm-cov output]
    ```

    **Step 5: Check for code smells (BUGF-02).**
    Review the coverage report for:
    - Files with 0% coverage that should have tests (not test-helpers or generated code)
    - Unreachable code paths indicated by coverage gaps
    - Any functions that are never called (dead code missed by clippy)
    Document findings in the coverage report and fix if straightforward.

    **Step 6: Run clippy one final time.**
    ```bash
    cargo clippy --workspace
    ```
    Ensure zero warnings remain after all test additions.
  </action>
  <verify>
    <automated>cargo test --workspace && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --workspace` exits 0 with all tests passing
    - `cargo clippy --workspace -- -D warnings` exits 0
    - .planning/phases/05-test-coverage-verification/coverage-report.txt exists
    - coverage-report.txt contains "Phase 1 Baseline: 75.17%"
    - coverage-report.txt contains "Phase 5 Result:" with a percentage
    - coverage-report.txt contains "Delta:" with a positive or zero value
  </acceptance_criteria>
  <done>Coverage measured, compared against 75.17% baseline, report saved. All workspace tests pass. Zero clippy warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Coverage measurement is read-only analysis — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | I (Info Disclosure) | coverage report | accept | Coverage report is in .planning/ (not shipped), contains no secrets |
</threat_model>

<verification>
1. `cargo test --workspace` — all tests pass
2. `cargo clippy --workspace -- -D warnings` — zero warnings
3. Coverage report exists with baseline comparison
</verification>

<success_criteria>
Coverage report shows improvement over Phase 1 baseline (ROADMAP Success Criteria #4). All workspace tests pass. Zero clippy warnings.
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-coverage-verification/05-03-SUMMARY.md`
</output>
