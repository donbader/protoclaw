---
phase: 05-test-coverage-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/anyclaw-core/src/health.rs
  - crates/anyclaw-sdk-channel/src/lib.rs
  - crates/anyclaw-sdk-tool/src/lib.rs
  - crates/anyclaw-agents/src/acp_types.rs
  - crates/anyclaw-agents/src/backend.rs
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
  - BUGF-01

must_haves:
  truths:
    - "health.rs has tests for HealthStatus, AgentHealth, HealthSnapshot serialization and Default impl"
    - "sdk-tool lib.rs has tests verifying re-exports are accessible"
    - "agents acp_types.rs has tests verifying re-exports from sdk-types work"
    - "agents backend.rs has tests for ProcessBackend trait object safety"
    - "sdk-channel LSP error is resolved — cargo check -p anyclaw-sdk-channel succeeds"
    - "All new tests use rstest with BDD naming"
  artifacts:
    - path: "crates/anyclaw-core/src/health.rs"
      provides: "Unit tests for HealthSnapshot, HealthStatus, AgentHealth"
      contains: "mod tests"
    - path: "crates/anyclaw-sdk-tool/src/lib.rs"
      provides: "Re-export verification tests"
      contains: "mod tests"
    - path: "crates/anyclaw-agents/src/acp_types.rs"
      provides: "Re-export verification tests"
      contains: "mod tests"
    - path: "crates/anyclaw-agents/src/backend.rs"
      provides: "Trait object safety tests"
      contains: "mod tests"
  key_links:
    - from: "crates/anyclaw-agents/src/acp_types.rs"
      to: "crates/anyclaw-sdk-types/src/acp.rs"
      via: "pub use re-export"
      pattern: "pub use anyclaw_sdk_types::acp"
---

<objective>
Fill all identified unit test gaps and fix the sdk-channel LSP bug.

Purpose: TEST-01 through TEST-05 require tests for specific untested files. BUGF-01 requires fixing the sdk-channel module resolution error introduced in Phase 4.
Output: Tests added inline to 5 files, LSP bug fixed, `cargo test` passes for all affected crates.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-test-coverage-verification/05-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix sdk-channel LSP bug + add unit tests for health.rs, sdk-tool lib.rs, acp_types.rs, backend.rs</name>
  <files>
    crates/anyclaw-sdk-channel/src/lib.rs
    crates/anyclaw-core/src/health.rs
    crates/anyclaw-sdk-tool/src/lib.rs
    crates/anyclaw-agents/src/acp_types.rs
    crates/anyclaw-agents/src/backend.rs
  </files>
  <read_first>
    crates/anyclaw-sdk-channel/src/lib.rs
    crates/anyclaw-sdk-channel/src/jsonrpc.rs
    crates/anyclaw-core/src/health.rs
    crates/anyclaw-sdk-tool/src/lib.rs
    crates/anyclaw-sdk-tool/src/trait_def.rs
    crates/anyclaw-sdk-tool/src/server.rs
    crates/anyclaw-agents/src/acp_types.rs
    crates/anyclaw-agents/src/backend.rs
    crates/anyclaw-agents/src/error.rs
    crates/anyclaw-sdk-types/src/acp.rs
    AGENTS.md
  </read_first>
  <behavior>
    BUGF-01 — sdk-channel LSP fix:
    - Investigate why `mod jsonrpc;` in sdk-channel/src/lib.rs causes "unresolved module" LSP error
    - The file `crates/anyclaw-sdk-channel/src/jsonrpc.rs` exists on disk
    - Likely cause: cargo check may pass but rust-analyzer fails due to cfg or path issue
    - Fix: ensure the module declaration is correct and rust-analyzer can resolve it
    - Test: `cargo check -p anyclaw-sdk-channel` succeeds with no errors

    TEST-01 — health.rs tests:
    - Test 1: HealthStatus::Healthy serializes to "healthy" (serde rename_all lowercase)
    - Test 2: HealthStatus::Degraded serializes to "degraded"
    - Test 3: HealthStatus round-trips through serde
    - Test 4: AgentHealth round-trips through serde (camelCase fields)
    - Test 5: HealthSnapshot round-trips through serde
    - Test 6: HealthSnapshot::default() returns Healthy status with empty vecs
    - Test 7: HealthSnapshot with agents/channels/mcp_servers populated round-trips

    TEST-03 — sdk-tool lib.rs tests:
    - Test 1: ToolSdkError is accessible via crate re-export
    - Test 2: ToolServer is accessible via crate re-export
    - Test 3: Tool trait is accessible via crate re-export
    - Test 4: DynTool type alias is accessible via crate re-export

    TEST-04 — acp_types.rs tests:
    - Test 1: InitializeParams is accessible via re-export from anyclaw_agents::acp_types
    - Test 2: SessionUpdateEvent is accessible via re-export
    - Test 3: ContentPart is accessible via re-export
    - Test 4: SessionNewParams is accessible via re-export

    TEST-04 — backend.rs tests:
    - Test 1: ProcessBackend is object-safe (can create Box<dyn ProcessBackend>)
    - Test 2: A mock implementation of ProcessBackend compiles and can be used as trait object
  </behavior>
  <action>
    **BUGF-01 — Fix sdk-channel LSP error:**
    Investigate `crates/anyclaw-sdk-channel/src/lib.rs` line 33: `mod jsonrpc;`. The file exists at `crates/anyclaw-sdk-channel/src/jsonrpc.rs`. Run `cargo check -p anyclaw-sdk-channel` to see if it's a real compilation error or LSP-only. If it's LSP-only (cargo check passes), the fix may be a rust-analyzer cache issue — document it. If it's a real error, fix the module path or declaration.

    **TEST-01 — Add tests to health.rs:**
    Add a `#[cfg(test)] mod tests` block at the bottom of `crates/anyclaw-core/src/health.rs`. Use `rstest` (already a dev-dependency of anyclaw-core). Tests per D-02 BDD naming:
    - `when_health_status_healthy_serialized_then_lowercase_string` — assert `serde_json::to_value(&HealthStatus::Healthy).unwrap() == json!("healthy")`
    - `when_health_status_degraded_serialized_then_lowercase_string` — assert `json!("degraded")`
    - `when_health_status_round_trips_then_identical` — parameterized with `#[case::healthy]` and `#[case::degraded]`
    - `when_agent_health_round_trips_then_identical` — create AgentHealth { name: "test-agent", connected: true, session_count: 3 }, serialize → deserialize, assert equal
    - `when_health_snapshot_default_then_healthy_with_empty_vecs` — assert default().status == Healthy, agents/channels/mcp_servers all empty
    - `when_health_snapshot_populated_round_trips_then_identical` — create snapshot with agents, channels, mcp_servers populated, round-trip

    **TEST-03 — Add tests to sdk-tool lib.rs:**
    Add `#[cfg(test)] mod tests` block at bottom of `crates/anyclaw-sdk-tool/src/lib.rs`. Tests:
    - `when_tool_sdk_error_reexported_then_accessible` — construct `ToolSdkError::ExecutionFailed("test".into())`, assert Display works
    - `when_tool_trait_reexported_then_accessible` — reference `Tool` trait as a type bound in a function signature, assert compiles
    - `when_dyn_tool_alias_reexported_then_accessible` — reference `DynTool` type, assert it's a valid type alias

    **TEST-04 — Add tests to acp_types.rs:**
    Add `#[cfg(test)] mod tests` block to `crates/anyclaw-agents/src/acp_types.rs`. Tests:
    - `when_initialize_params_reexported_then_constructible` — construct InitializeParams with protocol_version: 1, capabilities, options: None
    - `when_session_update_event_reexported_then_constructible` — construct SessionUpdateEvent with Result variant
    - `when_content_part_reexported_then_constructible` — construct ContentPart::text("hello")
    - `when_session_new_params_reexported_then_constructible` — construct SessionNewParams

    **TEST-04 — Add tests to backend.rs:**
    Add `#[cfg(test)] mod tests` block to `crates/anyclaw-agents/src/backend.rs`. Tests:
    - `when_process_backend_used_as_trait_object_then_compiles` — create a mock struct implementing ProcessBackend, box it as `Box<dyn ProcessBackend>`, assert is_alive() returns expected value
    - The mock struct needs: `is_alive() -> true`, `take_stdin/stdout/stderr() -> None`, `kill() -> Ok(())`, `wait() -> Ok(ExitStatus)` — use `std::process::Command::new("true").status()` for ExitStatus in tests

    All tests use `use rstest::rstest;` and `#[rstest]` attribute per D-02.
  </action>
  <verify>
    <automated>cargo test -p anyclaw-core -- health && cargo test -p anyclaw-sdk-tool && cargo test -p anyclaw-agents -- acp_types && cargo test -p anyclaw-agents -- backend && cargo check -p anyclaw-sdk-channel</automated>
  </verify>
  <acceptance_criteria>
    - crates/anyclaw-core/src/health.rs contains `mod tests` with at least 6 test functions
    - crates/anyclaw-sdk-tool/src/lib.rs contains `mod tests` with at least 3 test functions
    - crates/anyclaw-agents/src/acp_types.rs contains `mod tests` with at least 4 test functions
    - crates/anyclaw-agents/src/backend.rs contains `mod tests` with at least 1 test function
    - All test function names follow BDD pattern: `when_*_then_*` or `given_*_when_*_then_*`
    - All tests use `#[rstest]` attribute
    - `cargo check -p anyclaw-sdk-channel` exits 0
    - `cargo test -p anyclaw-core -- health` exits 0
    - `cargo test -p anyclaw-sdk-tool` exits 0
    - `cargo test -p anyclaw-agents -- acp_types` exits 0
    - `cargo test -p anyclaw-agents -- backend` exits 0
  </acceptance_criteria>
  <done>All identified test gap files have inline tests using rstest + BDD naming. sdk-channel LSP bug is resolved or documented. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Verify existing sdk-agent and sdk-tool error.rs tests satisfy TEST-02 and TEST-03</name>
  <files>
    crates/anyclaw-sdk-agent/src/error.rs
    crates/anyclaw-sdk-tool/src/error.rs
  </files>
  <read_first>
    crates/anyclaw-sdk-agent/src/error.rs
    crates/anyclaw-sdk-tool/src/error.rs
  </read_first>
  <action>
    Phase 4 already added tests to both `sdk-agent/error.rs` and `sdk-tool/error.rs`. Verify these tests:
    1. Exist and pass: `cargo test -p anyclaw-sdk-agent -- error` and `cargo test -p anyclaw-sdk-tool -- error`
    2. Use rstest with BDD naming per D-02 — check that test functions use `#[rstest]` and `when_*_then_*` naming
    3. Cover all variants of each error enum

    For `AgentSdkError` (3 variants: Protocol, Transform, Serde):
    - Verify tests exist for: display output of each variant, From<serde_json::Error> conversion, std::error::Error impl

    For `ToolSdkError` (4 variants: ExecutionFailed, InvalidInput, Io, Serde):
    - Verify tests exist for: display output of each variant, From<std::io::Error> conversion, From<serde_json::Error> conversion, std::error::Error impl

    If any test is missing or doesn't follow BDD naming, add it. If all tests exist and follow conventions, this task is a verification-only pass — no code changes needed.
  </action>
  <verify>
    <automated>cargo test -p anyclaw-sdk-agent -- error && cargo test -p anyclaw-sdk-tool -- error</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p anyclaw-sdk-agent -- error` exits 0 with at least 4 tests passing
    - `cargo test -p anyclaw-sdk-tool -- error` exits 0 with at least 5 tests passing
    - All test functions in both files use `#[rstest]` attribute
    - All test function names match `when_*_then_*` pattern
  </acceptance_criteria>
  <done>TEST-02 and TEST-03 (error.rs portions) are verified complete with rstest + BDD naming. All error enum variants have test coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan adds tests only — no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T (Tampering) | test code | accept | Tests are dev-only (#[cfg(test)]), not compiled into production binary |
</threat_model>

<verification>
1. `cargo test -p anyclaw-core -- health` — all health.rs tests pass
2. `cargo test -p anyclaw-sdk-tool` — all sdk-tool tests pass (lib.rs + error.rs)
3. `cargo test -p anyclaw-agents -- acp_types` — all acp_types tests pass
4. `cargo test -p anyclaw-agents -- backend` — all backend tests pass
5. `cargo test -p anyclaw-sdk-agent -- error` — all sdk-agent error tests pass
6. `cargo check -p anyclaw-sdk-channel` — no compilation errors
</verification>

<success_criteria>
Every file listed in ROADMAP Phase 5 Success Criteria #1 has tests. All tests use rstest 0.23 with BDD naming (Success Criteria #2). sdk-channel LSP bug resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-coverage-verification/05-01-SUMMARY.md`
</output>
