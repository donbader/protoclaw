---
phase: 06-file-decomposition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/anyclaw-supervisor/src/lib.rs
  - crates/anyclaw-supervisor/src/shutdown.rs
  - crates/anyclaw-supervisor/src/health.rs
  - crates/anyclaw-supervisor/src/factory.rs
autonomous: true
requirements:
  - DECO-02
  - DECO-03
  - BUGF-01
  - BUGF-02

must_haves:
  truths:
    - "supervisor production code is split across 4 files, none exceeding ~500 lines"
    - "public API of anyclaw-supervisor crate is unchanged — same exports from lib.rs"
    - "all existing tests pass without modification"
    - "cargo clippy --workspace produces zero warnings"
  artifacts:
    - path: "crates/anyclaw-supervisor/src/shutdown.rs"
      provides: "Signal handling and ordered shutdown logic"
      contains: "shutdown_signal"
    - path: "crates/anyclaw-supervisor/src/health.rs"
      provides: "Health snapshot refresh and restart monitoring"
      contains: "refresh_health_snapshot"
    - path: "crates/anyclaw-supervisor/src/factory.rs"
      provides: "Manager creation factory, session store builder, ManagerKind enum"
      contains: "create_manager"
    - path: "crates/anyclaw-supervisor/src/lib.rs"
      provides: "Supervisor struct, constructor, run/run_with_cancel, boot_managers"
      contains: "pub struct Supervisor"
  key_links:
    - from: "crates/anyclaw-supervisor/src/lib.rs"
      to: "crates/anyclaw-supervisor/src/shutdown.rs"
      via: "use crate::shutdown"
      pattern: "crate::shutdown"
    - from: "crates/anyclaw-supervisor/src/lib.rs"
      to: "crates/anyclaw-supervisor/src/health.rs"
      via: "use crate::health"
      pattern: "crate::health"
    - from: "crates/anyclaw-supervisor/src/lib.rs"
      to: "crates/anyclaw-supervisor/src/factory.rs"
      via: "use crate::factory"
      pattern: "crate::factory"
---

<objective>
Decompose the 951-line `anyclaw-supervisor/src/lib.rs` into focused modules.

Purpose: The supervisor is the second-largest file. Breaking it into modules separates signal handling, health monitoring, and manager factory logic from the core orchestration loop.

Output: 4 source files (lib.rs + 3 extracted modules), all under ~500 lines of production code, with unchanged public API.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/anyclaw-supervisor/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shutdown, health, and factory modules from lib.rs</name>
  <files>
    crates/anyclaw-supervisor/src/shutdown.rs
    crates/anyclaw-supervisor/src/health.rs
    crates/anyclaw-supervisor/src/factory.rs
    crates/anyclaw-supervisor/src/lib.rs
  </files>
  <read_first>
    crates/anyclaw-supervisor/src/lib.rs
    crates/anyclaw-supervisor/src/admin_server.rs
    AGENTS.md
  </read_first>
  <action>
Extract three modules from lib.rs. All extracted items get `pub(crate)` visibility. The Supervisor struct, its constructor, `run()`, `run_with_cancel()`, and `boot_managers()` stay in lib.rs.

**Module 1: `shutdown.rs`** — signal handling and ordered shutdown.

Move the free function:
- `shutdown_signal()` (line ~70) — make `pub(crate) async fn`

Move this `impl Supervisor` method as an `impl Supervisor` block in shutdown.rs:
- `shutdown_ordered(&self, slots: &mut [ManagerSlot], per_manager_timeout: Duration)` (line ~266) — make `pub(crate)`

The `ManagerSlot` struct is used by shutdown.rs — it must be visible. Keep `ManagerSlot` in lib.rs and make it `pub(crate)`. shutdown.rs imports it via `use crate::ManagerSlot`.

**Module 2: `health.rs`** — health monitoring and crash-restart logic.

Move these `impl Supervisor` methods as an `impl Supervisor` block in health.rs:
- `check_and_restart_managers(&mut self, slots: &mut [ManagerSlot], root_cancel: &CancellationToken)` (line ~291) — make `pub(crate)`
- `refresh_health_snapshot(&self, slots: &[ManagerSlot])` (line ~386) — make `pub(crate)`

health.rs needs access to `ManagerSlot`, `ManagerKind`, `create_manager`, and `Supervisor` fields. Since `ManagerKind` and `create_manager` will be in factory.rs, health.rs imports from both `crate::ManagerSlot` and `crate::factory::{ManagerKind, create_manager}`.

**Module 3: `factory.rs`** — manager creation factory and session store builder.

Move the free functions:
- `build_session_store(config: &SessionStoreConfig) -> Arc<dyn DynSessionStore>` (line ~447) — make `pub(crate) fn`
- `create_manager(name, config, tools_tx, tools_rx, agents_cmd_tx, ce_tx, ce_rx) -> ManagerKind` (line ~468) — make `pub(crate) fn`

Move the enum and its impl:
- `enum ManagerKind` (line ~522) — make `pub(crate) enum`
- `impl ManagerKind` with `start()` and `run()` methods (line ~528) — make methods `pub(crate)`

**What stays in lib.rs:**
- `#![warn(missing_docs)]` and crate-level doc comment
- All `use` imports needed by remaining code
- `pub mod admin_server;` (existing)
- New `pub(crate) mod` declarations for the three new modules
- `SupervisorError` enum (public API)
- `Supervisor` struct (public API)
- `ManagerSlot` struct — make `pub(crate)` (used by shutdown.rs, health.rs)
- `MANAGER_ORDER` constant with its LIMITATION comment — make `pub(crate)` (used by health.rs for restart)
- `impl Supervisor`: `new()`, `debug_http_port_rx()`, `with_boot_notify()`, `run()`, `run_with_cancel()`, `boot_managers()`
- All `#[cfg(test)] mod tests` — tests stay in lib.rs

**Update lib.rs module declarations** (add after `pub mod admin_server;`):
```rust
/// Signal handling and ordered manager shutdown.
pub(crate) mod shutdown;
/// Health snapshot refresh and crash-restart monitoring.
pub(crate) mod health;
/// Manager creation factory, session store builder, and ManagerKind dispatch enum.
pub(crate) mod factory;
```

**Update `run_with_cancel()`** to call the extracted functions:
- Replace inline `shutdown_signal().await` reference in `run()` with `crate::shutdown::shutdown_signal().await` (or `shutdown::shutdown_signal`)
- Replace `self.shutdown_ordered(...)` calls with `self.shutdown_ordered(...)` (still works — impl block in shutdown.rs extends Supervisor)
- Replace `self.check_and_restart_managers(...)` calls similarly
- Replace `create_manager(...)` calls in `boot_managers()` with `factory::create_manager(...)`
- Replace `ManagerKind` references with `factory::ManagerKind`

**Update `boot_managers()`** to use `factory::create_manager(...)` and `factory::ManagerKind`.

**Import management:** Each new module file needs its own `use` block. Remove corresponding imports from lib.rs that are no longer needed there.

**Critical: Do NOT change any function signatures, logic, or behavior.** This is a pure move operation. The only changes are visibility and import paths.
  </action>
  <verify>
    <automated>cd /Users/corey/Projects/anyclaw && cargo build --workspace 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `crates/anyclaw-supervisor/src/shutdown.rs` exists and contains `pub(crate) async fn shutdown_signal`
    - `crates/anyclaw-supervisor/src/shutdown.rs` contains `pub(crate) async fn shutdown_ordered`
    - `crates/anyclaw-supervisor/src/health.rs` exists and contains `pub(crate) async fn check_and_restart_managers`
    - `crates/anyclaw-supervisor/src/health.rs` contains `pub(crate) async fn refresh_health_snapshot`
    - `crates/anyclaw-supervisor/src/factory.rs` exists and contains `pub(crate) fn create_manager`
    - `crates/anyclaw-supervisor/src/factory.rs` contains `pub(crate) fn build_session_store`
    - `crates/anyclaw-supervisor/src/factory.rs` contains `pub(crate) enum ManagerKind`
    - `crates/anyclaw-supervisor/src/lib.rs` contains `pub struct Supervisor`
    - `crates/anyclaw-supervisor/src/lib.rs` contains `pub(crate) mod shutdown`
    - `crates/anyclaw-supervisor/src/lib.rs` contains `pub(crate) mod health`
    - `crates/anyclaw-supervisor/src/lib.rs` contains `pub(crate) mod factory`
    - `crates/anyclaw-supervisor/src/lib.rs` contains `pub(crate) struct ManagerSlot`
    - No `mod.rs` files created
    - `cargo build --workspace` succeeds with zero errors
  </acceptance_criteria>
  <done>Three new module files exist. lib.rs production code is reduced. Workspace builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Verify line counts, clippy, and all tests pass</name>
  <files>
    crates/anyclaw-supervisor/src/lib.rs
    crates/anyclaw-supervisor/src/shutdown.rs
    crates/anyclaw-supervisor/src/health.rs
    crates/anyclaw-supervisor/src/factory.rs
  </files>
  <read_first>
    crates/anyclaw-supervisor/src/lib.rs
    crates/anyclaw-supervisor/src/shutdown.rs
    crates/anyclaw-supervisor/src/health.rs
    crates/anyclaw-supervisor/src/factory.rs
  </read_first>
  <action>
**Step 1: Verify line counts.**
Run `wc -l` on all four supervisor source files. Production code (non-test lines) in each file should be under ~500 lines. The lib.rs file will still contain the `#[cfg(test)] mod tests` block (~350 lines of tests) — that's expected. Verify that the production code portion of lib.rs (lines before `#[cfg(test)]`) is under ~500 lines.

If any production code file exceeds ~500 lines, identify what can be further extracted and do so.

**Step 2: Run clippy.**
`cargo clippy --workspace` must produce zero warnings. Fix any new warnings introduced by the module extraction.

**Step 3: Run all tests.**
`cargo test --workspace` must pass. Per D-06, all existing tests pass without modification. If any test fails, the decomposition broke something — fix the extraction, do NOT modify the test.

**Step 4: Run cargo fmt.**
`cargo fmt --check` must pass.

**Step 5: Verify public API unchanged.**
Check that `lib.rs` still exports the same public items:
- `pub mod admin_server` — still present
- `pub enum SupervisorError` — still present
- `pub struct Supervisor` — still present
- `pub fn new()`, `pub async fn run()`, `pub async fn run_with_cancel()`, `pub fn debug_http_port_rx()` — still present

The new modules are `pub(crate)` — they do NOT appear in the public API.
  </action>
  <verify>
    <automated>cd /Users/corey/Projects/anyclaw && cargo clippy --workspace 2>&1 | tail -3 && cargo test --workspace 2>&1 | tail -5 && cargo fmt --check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l crates/anyclaw-supervisor/src/shutdown.rs` shows file exists with content
    - `wc -l crates/anyclaw-supervisor/src/health.rs` shows file exists with content
    - `wc -l crates/anyclaw-supervisor/src/factory.rs` shows file exists with content
    - Production code in lib.rs (before `#[cfg(test)]`) is under ~500 lines
    - `cargo clippy --workspace` exits 0 with no warnings
    - `cargo test --workspace` exits 0 with all tests passing
    - `cargo fmt --check` exits 0
    - `lib.rs` still exports `pub struct Supervisor`, `pub enum SupervisorError`, `pub mod admin_server`
  </acceptance_criteria>
  <done>All production code files under ~500 lines. Zero clippy warnings. All tests pass unchanged. Public API verified unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure structural refactoring — no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | T (Tampering) | factory.rs | accept | Manager creation logic preserved exactly — no new configuration paths |
| T-06-04 | D (Denial of Service) | shutdown.rs | accept | Shutdown ordering and timeout logic preserved exactly — no behavioral changes |
</threat_model>

<verification>
1. `cargo build --workspace` — compiles cleanly
2. `cargo clippy --workspace` — zero warnings
3. `cargo test --workspace` — all tests pass (no test modifications allowed per D-06)
4. `cargo fmt --check` — formatting clean
5. `wc -l` on all supervisor source files — no production code file exceeds ~500 lines
6. Public API surface unchanged (SupervisorError, Supervisor, admin_server)
</verification>

<success_criteria>
- lib.rs decomposed into 4 files (lib.rs + shutdown.rs + health.rs + factory.rs)
- No production code file exceeds ~500 lines
- Public API surface of anyclaw-supervisor crate is unchanged
- All extracted modules use pub(crate) visibility
- All existing tests pass without modification
- Zero clippy warnings across workspace
</success_criteria>

<output>
After completion, create `.planning/phases/06-file-decomposition/06-02-SUMMARY.md`
</output>
