---
phase: 04-sdk-external-polish
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - ext/agents/mock-agent/src/main.rs
  - ext/channels/debug-http/src/main.rs
  - ext/channels/telegram/src/channel.rs
  - ext/channels/telegram/src/deliver.rs
  - ext/channels/telegram/src/main.rs
  - ext/channels/sdk-test-channel/src/main.rs
  - ext/tools/sdk-test-tool/src/main.rs
  - examples/01-fake-agent-telegram-bot/tools/system-info/src/main.rs
autonomous: true
requirements:
  - JSON-08
  - BUGF-01
  - BUGF-02

must_haves:
  truths:
    - "Zero serde_json::Value in ext/ binaries except D-03 documented boundaries"
    - "All ext/ channel binaries compile against updated Channel trait from Plan 01"
    - "All ext/ tool binaries compile against updated Tool trait from Plan 01"
    - "mock-agent binary compiles and handles typed ACP messages"
    - "cargo check --workspace passes with zero errors"
  artifacts:
    - path: "ext/channels/debug-http/src/main.rs"
      provides: "Typed debug-http channel"
      contains: "show_permission_prompt"
    - path: "ext/channels/telegram/src/channel.rs"
      provides: "Typed telegram channel"
      contains: "show_permission_prompt"
    - path: "ext/agents/mock-agent/src/main.rs"
      provides: "Typed mock agent"
      contains: "InitializeParams"
  key_links:
    - from: "ext/channels/debug-http/src/main.rs"
      to: "crates/anyclaw-sdk-channel/src/trait_def.rs"
      via: "impl Channel for DebugHttpChannel"
      pattern: "impl Channel"
    - from: "ext/channels/telegram/src/channel.rs"
      to: "crates/anyclaw-sdk-channel/src/trait_def.rs"
      via: "impl Channel for TelegramChannel"
      pattern: "impl Channel"
---

<objective>
Replace all serde_json::Value usages in ext/ binaries and examples with typed structs. Fix compilation errors from Plan 01's SDK trait changes.

Purpose: External binaries are reference implementations — they should demonstrate typed usage patterns for SDK consumers.
Output: All ext/ binaries and examples compile with typed SDK traits. Zero undocumented Value.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sdk-external-polish/04-CONTEXT.md
@.planning/phases/04-sdk-external-polish/04-01-SUMMARY.md

<!-- Source files to type -->
@ext/agents/mock-agent/src/main.rs
@ext/channels/debug-http/src/main.rs
@ext/channels/telegram/src/channel.rs
@ext/channels/telegram/src/deliver.rs
@ext/channels/sdk-test-channel/src/main.rs
@ext/tools/sdk-test-tool/src/main.rs
@examples/01-fake-agent-telegram-bot/tools/system-info/src/main.rs

<interfaces>
<!-- Updated SDK traits from Plan 01 — executor MUST read 04-01-SUMMARY.md for exact signatures -->

From crates/anyclaw-sdk-channel/src/trait_def.rs (after Plan 01):
```rust
pub trait Channel: Send + 'static {
    fn capabilities(&self) -> ChannelCapabilities;
    async fn on_initialize(&mut self, params: ChannelInitializeParams) -> Result<(), ChannelSdkError>;
    async fn on_ready(&mut self, outbound: Sender<ChannelSendMessage>, permission_tx: Sender<PermissionResponse>) -> Result<(), ChannelSdkError>;
    async fn deliver_message(&mut self, msg: DeliverMessage) -> Result<(), ChannelSdkError>;
    async fn show_permission_prompt(&mut self, req: ChannelRequestPermission) -> Result<(), ChannelSdkError>;
    async fn handle_unknown(&mut self, method: &str, params: ...) -> Result<..., ChannelSdkError>;
    async fn on_session_created(&mut self, msg: SessionCreated) -> Result<(), ChannelSdkError>;
}
```

Known compilation errors in ext/ (pre-existing from Phase 3 Channel trait changes):
- telegram/channel.rs: missing `show_permission_prompt`, has stale `request_permission`, wrong arg count on `on_ready`
- debug-http/main.rs: same pattern — missing `show_permission_prompt`, has stale `request_permission`, wrong arg count on `on_ready`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ext/ channel binaries — update to new Channel trait + type Value usages</name>
  <files>
    ext/channels/debug-http/src/main.rs
    ext/channels/telegram/src/channel.rs
    ext/channels/telegram/src/deliver.rs
    ext/channels/telegram/src/main.rs
    ext/channels/sdk-test-channel/src/main.rs
  </files>
  <read_first>
    ext/channels/debug-http/src/main.rs
    ext/channels/telegram/src/channel.rs
    ext/channels/telegram/src/deliver.rs
    ext/channels/telegram/src/main.rs
    ext/channels/sdk-test-channel/src/main.rs
    crates/anyclaw-sdk-channel/src/trait_def.rs
    crates/anyclaw-sdk-channel/src/harness.rs
    crates/anyclaw-sdk-types/src/channel.rs
    crates/anyclaw-sdk-types/src/permission.rs
  </read_first>
  <action>
    Fix all ext/ channel binaries to compile against the updated Channel trait from Plan 01, and replace Value usages with typed structs (per D-01).

    **debug-http (801 lines, 12 Value usages):**
    1. Rename `request_permission` → `show_permission_prompt` with signature `(&mut self, req: ChannelRequestPermission) -> Result<(), ChannelSdkError>`
    2. Update `on_ready` to accept both `outbound: Sender<ChannelSendMessage>` and `permission_tx: Sender<PermissionResponse>` — store both
    3. Replace `serde_json::Value` in message handling with typed `DeliverMessage`, `ChannelSendMessage`, `ContentKind` dispatch
    4. Replace any `serde_json::from_value` / `serde_json::to_value` with direct typed struct construction
    5. Use `ContentKind` enum for content dispatch instead of string matching on Value fields
    6. Remove `#[allow(clippy::disallowed_types)]` from main.rs after Value is eliminated (per D-02)
    7. Any remaining Value usages (e.g., DeliverMessage.content which is Value in sdk-types) get D-03 comments

    **telegram (channel.rs: 417 lines, 8 Value; deliver.rs: 1010 lines, 2 Value):**
    1. Same trait method renames as debug-http
    2. channel.rs: Replace Value in message handling with typed structs
    3. deliver.rs: Replace Value in content rendering with `ContentKind` dispatch — this file renders agent content to Telegram markdown, so it heavily processes DeliverMessage.content. Since content is Value (D-03 boundary in sdk-types), document remaining usages as D-03.
    4. main.rs: Remove `#[allow(clippy::disallowed_types)]` annotations (per D-02), replace with D-03 justifications where needed

    **sdk-test-channel (127 lines, 1 Value):**
    1. Update Channel trait impl to match new signatures
    2. Replace the single Value usage with typed struct

    IMPORTANT: After fixing each binary, run `cargo check -p {binary_name}` to verify compilation. The telegram binary depends on teloxide — ensure typed changes don't break the teloxide integration.
  </action>
  <verify>
    <automated>cargo check -p debug-http -p telegram -p sdk-test-channel</automated>
  </verify>
  <acceptance_criteria>
    - debug-http/main.rs contains `show_permission_prompt` (not `request_permission`)
    - debug-http/main.rs `on_ready` accepts both `outbound` and `permission_tx` params
    - telegram/channel.rs contains `show_permission_prompt` (not `request_permission`)
    - `rg 'serde_json::Value' ext/channels/debug-http/src/main.rs` returns only D-03 documented usages
    - `rg 'serde_json::Value' ext/channels/telegram/src/` returns only D-03 documented usages
    - `rg 'disallowed_types' ext/channels/` returns zero matches or only D-03 justified
    - `cargo check -p debug-http -p telegram -p sdk-test-channel` passes with zero errors
  </acceptance_criteria>
  <done>All ext/ channel binaries compile against updated Channel trait. Value usages replaced or D-03 documented. Compilation errors from Phase 3 resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Type ext/ agent + tool binaries and examples</name>
  <files>
    ext/agents/mock-agent/src/main.rs
    ext/tools/sdk-test-tool/src/main.rs
    examples/01-fake-agent-telegram-bot/tools/system-info/src/main.rs
  </files>
  <read_first>
    ext/agents/mock-agent/src/main.rs
    ext/tools/sdk-test-tool/src/main.rs
    examples/01-fake-agent-telegram-bot/tools/system-info/src/main.rs
    crates/anyclaw-sdk-types/src/acp.rs
    crates/anyclaw-sdk-tool/src/trait_def.rs
  </read_first>
  <action>
    Replace Value usages in remaining ext/ binaries and examples (per D-01).

    **mock-agent (613 lines, 3 Value usages):**
    - mock-agent is a standalone binary that speaks ACP over stdio — it does NOT use the sdk-agent crate
    - It manually constructs JSON-RPC messages. Replace `serde_json::Value` with typed ACP structs from `anyclaw-sdk-types`: `InitializeResult`, `SessionNewResult`, `SessionUpdateEvent`, `ContentPart`, etc.
    - Add `anyclaw-sdk-types` as a dependency in mock-agent's Cargo.toml if not already present
    - Replace `serde_json::json!({...})` construction with typed struct construction where ACP types exist
    - Any remaining Value usages (e.g., constructing JSON-RPC framing) get D-03 comments — mock-agent builds raw JSON-RPC messages, which is a legitimate boundary
    - Remove `#[allow(clippy::disallowed_types)]` (per D-02), replace with D-03 justifications

    **sdk-test-tool (97 lines, 2 Value usages):**
    - Implements the Tool trait — `input_schema()` returns Value and `execute()` takes/returns Value
    - These are D-03 boundaries (Tool trait is Value-based per Plan 01 Task 3)
    - Add D-03 comments, remove `#[allow(clippy::disallowed_types)]` with D-03 justification

    **system-info example (94 lines, 2 Value usages):**
    - Same pattern as sdk-test-tool — Tool trait is Value-based
    - Add D-03 comments, remove `#[allow(clippy::disallowed_types)]` with D-03 justification

    After all changes, run full workspace check to verify everything compiles together.
  </action>
  <verify>
    <automated>cargo check --workspace</automated>
  </verify>
  <acceptance_criteria>
    - mock-agent/main.rs uses typed ACP structs (contains `InitializeResult` or `SessionNewResult`)
    - `rg 'serde_json::Value' ext/agents/mock-agent/src/main.rs` returns only D-03 documented usages
    - `rg 'serde_json::Value' ext/tools/sdk-test-tool/src/main.rs` returns only D-03 documented usages
    - `rg 'disallowed_types' ext/agents/ ext/tools/ examples/` returns zero matches or only D-03 justified
    - `cargo check --workspace` passes with zero errors
    - `cargo clippy --workspace` passes with zero warnings
  </acceptance_criteria>
  <done>All ext/ binaries and examples use typed structs where possible. All remaining Value usages are D-03 documented. JSON-08 complete. Full workspace compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin JSON → ext binary | External binaries parse JSON-RPC from stdin — typed parsing catches malformed input |
| ext binary → stdout JSON | Typed struct serialization ensures well-formed output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | T (Tampering) | mock-agent ACP messages | mitigate | Typed ACP structs ensure mock-agent produces valid protocol messages — prevents test pollution from malformed mocks |
| T-04-07 | T (Tampering) | Channel stdin parsing | mitigate | Typed deserialization in channel binaries rejects malformed JSON-RPC at parse boundary |
</threat_model>

<verification>
- `cargo check --workspace` passes with zero errors
- `cargo clippy --workspace` passes with zero warnings
- `cargo test --workspace` passes
- `rg 'serde_json::Value' ext/ examples/` returns only D-03 documented usages
- `rg 'disallowed_types' ext/ examples/` returns zero matches or only D-03 justified
- All ext/ channel binaries implement `show_permission_prompt` (not stale `request_permission`)
</verification>

<success_criteria>
JSON-08, BUGF-01, BUGF-02 complete: All ext/ binaries and examples use typed SDK traits. Compilation errors from Phase 3 Channel trait changes resolved. Zero undocumented Value. Full workspace compiles and passes clippy.
</success_criteria>

<output>
After completion, create `.planning/phases/04-sdk-external-polish/04-04-SUMMARY.md`
</output>
