---
phase: 85-example-02-compose-profiles
plan: 02
type: execute
wave: 2
depends_on: ["85-01"]
files_modified:
  - examples/02-real-agents-telegram-bot/Dockerfile
  - examples/02-real-agents-telegram-bot/docker-compose.yml
  - examples/02-real-agents-telegram-bot/protoclaw.yaml
  - examples/02-real-agents-telegram-bot/test.sh
  - examples/02-real-agents-telegram-bot/README.md
  - examples/02-real-agents-telegram-bot/.env.example
autonomous: true
requirements:
  - PROF-04
  - TEST-03

must_haves:
  truths:
    - "Dockerfile has no opencode-wrapper or acp-bridge COPY statements"
    - "Dockerfile has no example-02-claude-code stage"
    - "opencode-agent stage ENTRYPOINT is ['opencode', 'acp']"
    - "protoclaw.yaml has single opencode agent with entrypoint array"
    - "protoclaw.yaml has no args field on any agent"
    - "test.sh has no --local flag or local mode logic"
    - "docker compose up -d builds and starts successfully"
    - "README documents how to switch to Claude Code or Kiro manually"
  artifacts:
    - path: "examples/02-real-agents-telegram-bot/Dockerfile"
      provides: "Simplified multi-stage build without wrapper/bridge/claude-code"
    - path: "examples/02-real-agents-telegram-bot/protoclaw.yaml"
      provides: "Single-agent config using entrypoint array"
      contains: "entrypoint:"
    - path: "examples/02-real-agents-telegram-bot/test.sh"
      provides: "Docker-only test suite"
    - path: "examples/02-real-agents-telegram-bot/README.md"
      provides: "Updated docs with agent switching instructions"
  key_links:
    - from: "examples/02-real-agents-telegram-bot/protoclaw.yaml"
      to: "examples/02-real-agents-telegram-bot/Dockerfile"
      via: "agent image name must match"
      pattern: "protoclaw-opencode-agent"
    - from: "examples/02-real-agents-telegram-bot/docker-compose.yml"
      to: "examples/02-real-agents-telegram-bot/Dockerfile"
      via: "build targets must exist in Dockerfile"
      pattern: "example-02-opencode"
---

<objective>
Update Example 02 for the new architecture: replace opencode-wrapper with direct `opencode acp` spawn, simplify Dockerfile, update config and tests, document agent switching.

Purpose: Example 02 becomes the reference implementation for the v0.3.1 architecture where agents use direct ACP mode instead of wrapper binaries (per D-01 through D-15).

Output: Working Example 02 with simplified Dockerfile, updated config, passing tests, and comprehensive README.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-example-02-compose-profiles/85-CONTEXT.md
@.planning/phases/85-example-02-compose-profiles/85-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 that this plan depends on -->

From crates/protoclaw-config/src/types.rs (after Plan 01):
```rust
pub struct StringOrArray(pub Vec<String>);

pub struct LocalWorkspaceConfig {
    pub binary: StringOrArray,  // accepts "opencode" or ["opencode", "acp"]
    pub working_dir: Option<PathBuf>,
    pub env: HashMap<String, String>,
}

pub struct DockerWorkspaceConfig {
    pub image: String,
    pub entrypoint: Option<StringOrArray>,  // accepts "opencode" or ["opencode", "acp"]
    // ... other fields unchanged
}

pub struct AgentConfig {
    pub workspace: WorkspaceConfig,
    // NO args field
    pub enabled: bool,
    pub tools: Vec<String>,
    // ...
}
```

Current Example 02 files (to be modified):
- Dockerfile: 77 lines, has opencode-wrapper COPY, claude-code stage, opencode-agent uses wrapper entrypoint
- docker-compose.yml: 59 lines, references example-02-opencode target
- protoclaw.yaml: 88 lines, has args fields, opencode-wrapper binary refs, opencode-local and claude-code agents
- test.sh: 185 lines, has --local flag and local mode logic
- README.md: 97 lines, references opencode-wrapper, three-agent switching table
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Dockerfile, docker-compose.yml, and protoclaw.yaml</name>
  <files>examples/02-real-agents-telegram-bot/Dockerfile, examples/02-real-agents-telegram-bot/docker-compose.yml, examples/02-real-agents-telegram-bot/protoclaw.yaml</files>
  <read_first>
    - examples/02-real-agents-telegram-bot/Dockerfile (current multi-stage build)
    - examples/02-real-agents-telegram-bot/docker-compose.yml (current topology)
    - examples/02-real-agents-telegram-bot/protoclaw.yaml (current agent configs with args)
    - examples/02-real-agents-telegram-bot/.env.example (current env template)
  </read_first>
  <action>
**Dockerfile changes (per D-08, D-09, D-10, D-11):**

1. Keep `FROM ghcr.io/donbader/protoclaw-builder:latest AS builder` (unchanged).
2. Keep `opencode-deps` stage (unchanged — still needed for opencode-ai npm install).
3. **Remove entire `claude-code-deps` stage** (per D-10) — lines 16-25 deleted entirely.
4. **Update `example-02-opencode` stage** (per D-11):
   - Remove the line `COPY --from=builder /usr/local/bin/agents/opencode-wrapper /usr/local/bin/agents/opencode-wrapper`
   - Do NOT add any acp-bridge COPY (per D-09 — neither wrapper nor bridge needed)
   - Keep all other COPY lines (protoclaw, debug-http, telegram, system-info)
5. **Remove entire `example-02-claude-code` stage** (per D-10) — lines 42-54 deleted entirely.
6. **Update `opencode-agent` stage** (per D-08):
   - Remove the line `COPY --from=builder /usr/local/bin/agents/opencode-wrapper /usr/local/bin/agents/opencode-wrapper`
   - Change `ENTRYPOINT ["/usr/local/bin/agents/opencode-wrapper"]` → `ENTRYPOINT ["opencode", "acp"]`
   - opencode binary is already on PATH from the opencode-deps COPY, so bare `opencode` works

The resulting Dockerfile should have exactly 3 stages after builder: `opencode-deps`, `example-02-opencode`, `opencode-agent`.

**docker-compose.yml changes:**
- No structural changes needed — it already targets `example-02-opencode` and `opencode-agent`.
- Remove `LOG_LEVEL`, `PORT`, `HOST`, `DOCKER_HOST` from environment section (these are set in protoclaw.yaml via SubstYaml, not env vars — the env vars are redundant and ignored).
- Keep `env_file: .env` (for TELEGRAM_BOT_TOKEN).

**protoclaw.yaml changes (per D-12, D-13):**

Replace the entire agents section with a single agent using the new array entrypoint syntax:

```yaml
agents_manager:
  acp_timeout_secs: 120
  agents:
    opencode:
      workspace:
        type: docker
        image: "protoclaw-opencode-agent:latest"
        entrypoint: ["opencode", "acp"]
        docker_host: "tcp://socket-proxy:2375"
        network: "protoclaw-external"
        working_dir: "/home/node"
        env:
          XDG_CONFIG_HOME: "/home/node/.config"
          XDG_DATA_HOME: "/home/node/.local/share"
      pull_policy: "Never"
      enabled: true
      tools:
        - "system-info"
```

Key changes:
- `entrypoint` changes from `"@built-in/agents/acp-bridge"` to `["opencode", "acp"]` (per D-01, D-12)
- Remove `opencode-local` agent definition entirely
- Remove `claude-code` agent definition entirely
- Remove all `args:` fields (field no longer exists per D-06)
- Channel `agent` fields stay as `"opencode"` (per D-13)
- All other config (channels, tools, supervisor) stays the same

Update the comment at the top to reflect the new architecture:
```yaml
# Protoclaw Real Agent Example — Docker Workspace
# OpenCode runs in an isolated container managed by protoclaw via bollard.
# The agent uses direct ACP mode (opencode acp) — no wrapper binary needed.
# See README.md for switching to Claude Code or Kiro.
```
  </action>
  <verify>
    <automated>cd examples/02-real-agents-telegram-bot && docker compose config --quiet 2>&1 && echo "compose-valid"</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile does NOT contain string "opencode-wrapper"
    - Dockerfile does NOT contain string "acp-bridge"
    - Dockerfile does NOT contain string "claude-code-deps"
    - Dockerfile does NOT contain string "example-02-claude-code"
    - Dockerfile contains `ENTRYPOINT ["opencode", "acp"]` in opencode-agent stage
    - protoclaw.yaml contains `entrypoint: ["opencode", "acp"]`
    - protoclaw.yaml does NOT contain `args:`
    - protoclaw.yaml does NOT contain `opencode-local:`
    - protoclaw.yaml does NOT contain `claude-code:`
    - docker-compose.yml passes `docker compose config --quiet`
  </acceptance_criteria>
  <done>Dockerfile simplified to 3 stages, protoclaw.yaml has single agent with array entrypoint, docker-compose.yml validates</done>
</task>

<task type="auto">
  <name>Task 2: Update test.sh, README.md, and .env.example</name>
  <files>examples/02-real-agents-telegram-bot/test.sh, examples/02-real-agents-telegram-bot/README.md, examples/02-real-agents-telegram-bot/.env.example</files>
  <read_first>
    - examples/02-real-agents-telegram-bot/test.sh (current test suite with --local mode)
    - examples/02-real-agents-telegram-bot/README.md (current docs with three-agent table)
    - examples/02-real-agents-telegram-bot/.env.example (current env template)
  </read_first>
  <action>
**test.sh changes (per D-14, D-15):**

1. Remove `--local` flag parsing (lines 17-21: `LOCAL_MODE=false`, the if block).
2. Remove local mode setup section (lines 52-66: the `if [ "$LOCAL_MODE" = true ]` block that patches protoclaw.yaml and creates docker-compose.override.yml).
3. Remove local mode conditional in build section (lines 69-72: `if [ "$LOCAL_MODE" = false ]` around agent image build).
4. Simplify cleanup function: remove `[ -f protoclaw.yaml.bak ] && mv protoclaw.yaml.bak protoclaw.yaml` and `[ -f docker-compose.override.yml ] && rm -f docker-compose.override.yml`.
5. Update usage comment at top: `# Usage: ./test.sh [base_url]` (remove `[--local]`).
6. Remove `ANTHROPIC_API_KEY` check/note (lines 47-49) — not relevant for the test flow.
7. Keep all test assertions unchanged (health, message, SSE, result, batch merging per D-15).
8. Remove `--profile build-only` from docker compose build command — just use `docker compose build` since there's no profile-gated build service.

The resulting test.sh should be ~140 lines (down from 185).

**README.md changes (per D-03):**

Rewrite to reflect the new architecture:

1. Title: "Example 02: Real Agent Bot"
2. Description: "A protoclaw bot with a real AI agent. OpenCode runs in an isolated Docker container using direct ACP mode."
3. Quick Start: `docker compose up -d` (unchanged)
4. Run Tests: `./test.sh` (remove `--local` mention)
5. Add Telegram section (unchanged)
6. Architecture diagram: Update to remove "opencode + wrapper" label, replace with "opencode acp"
7. **Switching Agents section** (per D-03): Replace the three-column table with manual switching instructions:

```markdown
## Switching Agents

OpenCode is the default agent. To use a different agent:

### Claude Code

1. Build a claude-code agent image (replace the opencode-agent stage in Dockerfile):
   ```dockerfile
   FROM node:20-slim AS opencode-agent
   RUN npm install -g @anthropic-ai/claude-code --omit=dev
   USER node
   WORKDIR /home/node
   ENTRYPOINT ["claude", "--acp"]
   ```
2. Update `protoclaw.yaml`: change `entrypoint: ["opencode", "acp"]` to `entrypoint: ["claude", "--acp"]`
3. Rebuild: `docker compose up --build -d`

### Kiro

1. Build a kiro agent image (replace the opencode-agent stage in Dockerfile with Kiro CLI installation)
2. Update `protoclaw.yaml`: change `entrypoint: ["opencode", "acp"]` to `entrypoint: ["kiro", "--acp"]` (verify Kiro's ACP flag)
3. Rebuild: `docker compose up --build -d`
```

8. Files table: Remove `.opencode/` row description about "baked into agent image", simplify to current state.

**.env.example** — no changes needed (already minimal with just TELEGRAM vars).
  </action>
  <verify>
    <automated>cd examples/02-real-agents-telegram-bot && bash -n test.sh && echo "syntax-ok"</automated>
  </verify>
  <acceptance_criteria>
    - test.sh does NOT contain string "--local"
    - test.sh does NOT contain string "LOCAL_MODE"
    - test.sh does NOT contain string "protoclaw.yaml.bak"
    - test.sh does NOT contain string "docker-compose.override"
    - test.sh contains "health" test (preserved per D-15)
    - test.sh contains "SSE" test (preserved per D-15)
    - test.sh contains "batch" or "merging" test (preserved per D-15)
    - `bash -n test.sh` exits 0 (valid syntax)
    - README.md contains "Claude Code" switching instructions
    - README.md contains "Kiro" switching instructions
    - README.md does NOT contain "opencode-wrapper"
    - README.md contains "opencode acp" or `["opencode", "acp"]`
  </acceptance_criteria>
  <done>test.sh simplified to Docker-only, README documents manual agent switching for Claude Code and Kiro, all files syntactically valid</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User .env → docker-compose | Environment variables (TELEGRAM_BOT_TOKEN) flow into containers |
| protoclaw.yaml → agent spawn | Config values determine what binary runs inside the agent container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-04 | T (Tampering) | Dockerfile ENTRYPOINT | accept | ENTRYPOINT is set at build time; runtime override requires docker-compose entrypoint override which is user-controlled config |
| T-85-05 | I (Information Disclosure) | .env file with API keys | accept | .env is gitignored; .env.example contains no secrets; same posture as before |
</threat_model>

<verification>
1. `docker compose config --quiet` validates compose file
2. `bash -n test.sh` validates shell syntax
3. Dockerfile has no opencode-wrapper or acp-bridge references
4. protoclaw.yaml parses with the new config schema (no args field)
5. README documents switching to Claude Code and Kiro
</verification>

<success_criteria>
Example 02 fully updated for v0.3.1 architecture: Dockerfile simplified (3 stages, no wrapper), protoclaw.yaml uses array entrypoint, test.sh is Docker-only, README documents agent switching.
</success_criteria>

<output>
After completion, create `.planning/phases/85-example-02-compose-profiles/85-02-SUMMARY.md`
</output>
