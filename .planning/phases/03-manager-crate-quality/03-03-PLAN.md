---
phase: 03-manager-crate-quality
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/anyclaw-agents/Cargo.toml
  - crates/anyclaw-agents/src/connection.rs
  - crates/anyclaw-agents/src/slot.rs
  - crates/anyclaw-agents/src/platform_commands.rs
autonomous: true
requirements: [JSON-04, ADVN-01, CLON-02, CLON-03, D-07, BUGF-01, BUGF-02]

must_haves:
  truths:
    - "Arc<Mutex<HashMap>> in agents connection.rs replaced with DashMap"
    - "Codec boundary shims (from_value/to_value) in agents connection.rs replaced with typed JsonRpcMessage pipeline"
    - "platform_commands.rs returns typed Vec instead of Value::Array"
    - "slot.rs last_available_commands uses typed Vec instead of Option<Value>"
    - "Clone count in connection.rs reduced"
  artifacts:
    - path: "crates/anyclaw-agents/src/connection.rs"
      provides: "DashMap for pending_requests, typed JsonRpcMessage pipeline, no codec shims"
    - path: "crates/anyclaw-agents/src/platform_commands.rs"
      provides: "Typed PlatformCommand struct, Vec<PlatformCommand> return"
    - path: "crates/anyclaw-agents/src/slot.rs"
      provides: "Typed last_available_commands field"
  key_links:
    - from: "crates/anyclaw-agents/src/connection.rs"
      to: "crates/anyclaw-jsonrpc/src/codec.rs"
      via: "NdJsonCodec produces JsonRpcMessage directly"
      pattern: "JsonRpcMessage"
    - from: "crates/anyclaw-agents/src/platform_commands.rs"
      to: "crates/anyclaw-agents/src/manager.rs"
      via: "platform_commands_json() consumed by manager"
      pattern: "platform_commands"
---

<objective>
Type the agents support files (connection.rs, slot.rs, platform_commands.rs) and migrate to DashMap.

Purpose: Prepare the agents crate foundation before tackling the 3,781-line manager.rs in Plan 04. Replace Arc<Mutex<HashMap>> with DashMap, remove Phase 2 codec boundary shims, type platform commands and slot fields. Per D-02, the connection consumes JsonRpcMessage directly from the codec — no intermediate Value conversion.

Output: Typed connection pipeline with DashMap, typed platform commands, typed slot fields. Manager.rs can then build on these typed foundations in Plan 04.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-manager-crate-quality/03-CONTEXT.md

<interfaces>
<!-- Phase 2 typed foundations -->

From crates/anyclaw-jsonrpc/src/types.rs:
```rust
pub enum JsonRpcMessage {
    Request(JsonRpcRequest),
    Response(JsonRpcResponse),
}
pub struct JsonRpcRequest {
    pub jsonrpc: String,
    pub method: String,
    pub params: Option<serde_json::Value>,  // D-03
    pub id: Option<RequestId>,
}
pub struct JsonRpcResponse {
    pub jsonrpc: String,
    pub result: Option<serde_json::Value>,  // D-03
    pub error: Option<JsonRpcError>,
    pub id: Option<RequestId>,
}
pub enum RequestId { Number(u64), String(String) }
```

Current codec shims in agents/connection.rs (TO BE REMOVED):
```rust
// line 93: let Ok(typed_msg) = serde_json::from_value::<JsonRpcMessage>(msg)
// line 193: let value = serde_json::to_value(&typed_msg).unwrap_or_default();
```

Current pending_requests type:
```rust
pending_requests: Arc<Mutex<HashMap<u64, oneshot::Sender<serde_json::Value>>>>
```

Current platform_commands return:
```rust
pub fn platform_commands_json() -> serde_json::Value {
    let cmds: Vec<serde_json::Value> = PLATFORM_COMMANDS.iter().map(|cmd| serde_json::json!({...})).collect();
    serde_json::Value::Array(cmds)
}
```

Current slot field:
```rust
pub(crate) last_available_commands: Option<serde_json::Value>,
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DashMap migration + typed codec pipeline in agents connection.rs</name>
  <files>crates/anyclaw-agents/Cargo.toml, crates/anyclaw-agents/src/connection.rs</files>
  <read_first>
    - crates/anyclaw-agents/Cargo.toml
    - crates/anyclaw-agents/src/connection.rs (full file — 632 lines, understand codec shims, pending_requests, IncomingAgentMessage, send_request, send_notification, send_raw)
    - crates/anyclaw-agents/AGENTS.md (connection architecture, bridge collapse, anti-patterns)
    - crates/anyclaw-jsonrpc/src/types.rs (JsonRpcMessage, JsonRpcRequest, JsonRpcResponse, RequestId)
    - crates/anyclaw-jsonrpc/src/codec.rs (NdJsonCodec Decoder/Encoder impls)
  </read_first>
  <action>
1. Add `dashmap = { workspace = true }` to `[dependencies]` in agents Cargo.toml.

2. Replace `Arc<Mutex<HashMap<u64, oneshot::Sender<serde_json::Value>>>>` with `Arc<DashMap<u64, oneshot::Sender<JsonRpcResponse>>>`:
   - Change the `pending_requests` field type in `AgentConnection` struct (line ~244)
   - Change all 5 occurrences of the type annotation (lines ~136, ~177, ~242, ~265, ~304)
   - Replace `.lock().await.insert(id, tx)` with `.insert(id, tx)`
   - Replace `.lock().await.remove(&id)` with `.remove(&id).map(|(_, v)| v)`
   - The oneshot sender now sends `JsonRpcResponse` instead of `Value`

3. Remove codec boundary shims:
   - Line ~93: `serde_json::from_value::<JsonRpcMessage>(msg)` — codec already produces `JsonRpcMessage`. Remove conversion. Match on `JsonRpcMessage::Request` and `JsonRpcMessage::Response` directly in the reader task.
   - Line ~193: `serde_json::to_value(&typed_msg).unwrap_or_default()` — codec accepts typed messages. Write `JsonRpcRequest` directly to framed writer.

4. Update `IncomingAgentMessage` enum (line ~24):
   - `AgentRequest(serde_json::Value)` → `AgentRequest(JsonRpcRequest)`
   - `AgentNotification(serde_json::Value)` → `AgentNotification(JsonRpcRequest)`
   - Update reader task to construct these from typed `JsonRpcMessage` variants

5. Update `send_request()` (line ~332):
   - Build a `JsonRpcRequest` struct directly instead of `serde_json::json!({...})` Value
   - Write the typed request to the framed writer
   - Response receiver now returns `JsonRpcResponse` instead of `Value`

6. Update `send_notification()` (line ~357):
   - Build `JsonRpcRequest` with `id: None` and write directly

7. Update `send_raw()` (line ~373):
   - Currently sends `serde_json::Value` — this is used for permission responses which are pre-built JSON-RPC responses. Change to accept `JsonRpcResponse` and write directly. Check all callers in manager.rs to understand what they pass.

8. CRITICAL: Preserve the bridge-collapse architecture. `spawn_with_bridge()` pushes `SlotIncoming { slot_idx, msg }` to the manager's shared channel. The `msg` field type changes from Value to the typed IncomingAgentMessage. Do NOT reintroduce intermediate forwarding channels.

9. Audit 2 clones in connection.rs — replace unnecessary ones.
  </action>
  <verify>
    <automated>cargo clippy -p anyclaw-agents -- -D warnings && cargo test -p anyclaw-agents</automated>
  </verify>
  <acceptance_criteria>
    - connection.rs contains `DashMap` import and uses it for pending_requests
    - connection.rs does NOT contain `serde_json::from_value` or `serde_json::to_value`
    - connection.rs does NOT contain `Arc<Mutex<HashMap`
    - IncomingAgentMessage variants use JsonRpcRequest instead of Value
    - send_request builds JsonRpcRequest struct, not json!({}) Value
    - send_raw accepts JsonRpcResponse (or JsonRpcMessage), not Value
    - Bridge-collapse architecture preserved (spawn_with_bridge still pushes to shared channel)
    - cargo clippy -p anyclaw-agents -- -D warnings exits 0
  </acceptance_criteria>
  <done>DashMap replaces Arc<Mutex<HashMap>>, codec shims removed, agents connection.rs uses typed JsonRpcMessage pipeline</done>
</task>

<task type="auto">
  <name>Task 2: Type platform_commands.rs + slot.rs, update lib.rs allows</name>
  <files>crates/anyclaw-agents/src/platform_commands.rs, crates/anyclaw-agents/src/slot.rs, crates/anyclaw-agents/src/lib.rs</files>
  <read_first>
    - crates/anyclaw-agents/src/platform_commands.rs (full file — small, ~40 lines)
    - crates/anyclaw-agents/src/slot.rs (full file — small, focus on last_available_commands field)
    - crates/anyclaw-agents/src/lib.rs
    - crates/anyclaw-agents/src/manager.rs (grep for `platform_commands` and `last_available_commands` to understand consumers)
  </read_first>
  <action>
1. In `platform_commands.rs`:
   - Create a `#[derive(Debug, Clone, Serialize)]` struct `PlatformCommand` with fields: `name: String`, `description: String`, `parameters: PlatformCommandParams` (or similar typed struct matching the current json!({}) shape).
   - Change `platform_commands_json() -> serde_json::Value` to `platform_commands() -> Vec<PlatformCommand>` returning typed structs.
   - The manager.rs injects these into `availableCommands` arrays in agent content. It will need to serialize them to Value at the injection point — that's fine, the source of truth is now typed.
   - Keep a convenience `platform_commands_value() -> serde_json::Value` that calls `serde_json::to_value(platform_commands()).unwrap_or_default()` for the manager.rs injection point. This function needs `#[allow(clippy::disallowed_types)]` with D-03 comment (serialization boundary for agent content mutation).
   - Remove the 3 direct Value usages (Value::Array, json!({}) per command).

2. In `slot.rs`:
   - Change `last_available_commands: Option<serde_json::Value>` to `last_available_commands: Option<Vec<PlatformCommand>>` (or `Option<serde_json::Value>` with D-03 comment if the manager stores arbitrary agent-reported commands here, not just platform commands).
   - Read how manager.rs sets this field to determine the correct type. If it stores the agent's reported availableCommands (which are arbitrary), it's D-03 and stays Value with comment. If it only stores platform commands, type it.

3. In `lib.rs`:
   - Update the 4 `#[allow(clippy::disallowed_types)]` annotations:
     - `connection` module: if now Value-free after DashMap + typed pipeline, REMOVE the allow
     - `platform_commands` module: if now Value-free (or only has the convenience serialization function), REMOVE or update
     - `slot` module: REMOVE if Value-free, or update comment to D-03 justification
     - `manager` module: keep — will be updated in Plan 04
  </action>
  <verify>
    <automated>cargo clippy -p anyclaw-agents -- -D warnings && cargo test -p anyclaw-agents && cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - platform_commands.rs contains `struct PlatformCommand` with Serialize derive
    - platform_commands.rs has a typed `platform_commands() -> Vec<PlatformCommand>` function
    - slot.rs last_available_commands is typed (or has D-03 comment if it must stay Value)
    - lib.rs allows updated — connection/platform_commands/slot allows removed or updated with D-03 justification
    - cargo build --workspace exits 0
  </acceptance_criteria>
  <done>Agents support files typed, lib.rs allows updated for connection/slot/platform_commands, workspace builds. Manager.rs ready for Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent subprocess → manager | Agent sends JSON-RPC messages (session/update, requests) over stdio |
| manager → agent subprocess | Manager sends ACP commands (initialize, session/prompt) over stdio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | T (Tampering) | connection.rs | accept | Agent subprocesses are admin-configured binaries — same trust as channels |
| T-03-06 | S (Spoofing) | connection.rs | accept | Agents communicate via piped stdio, not network — no spoofing vector |
</threat_model>

<verification>
```bash
cargo clippy -p anyclaw-agents -- -D warnings
cargo test -p anyclaw-agents
cargo build --workspace
```
</verification>

<success_criteria>
- `Arc<Mutex<HashMap<u64, oneshot::Sender<Value>>>>` replaced with `DashMap<u64, oneshot::Sender<JsonRpcResponse>>` in connection.rs
- Codec boundary shims (`serde_json::from_value`, `serde_json::to_value`) removed from connection.rs
- `platform_commands_json()` returns typed `Vec<PlatformCommand>` (or serializable struct), not `Value::Array`
- `slot.rs` `last_available_commands` uses typed `Vec<PlatformCommand>` instead of `Option<Value>`
- `cargo clippy -p anyclaw-agents -- -D warnings` passes
- `cargo test -p anyclaw-agents` passes
- `cargo build --workspace` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-manager-crate-quality/03-03-SUMMARY.md`
</output>
