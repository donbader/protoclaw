---
phase: 03-manager-crate-quality
plan: 04
type: execute
wave: 2
depends_on: [03]
files_modified:
  - crates/anyclaw-agents/src/manager.rs
  - crates/anyclaw-agents/src/lib.rs
  - crates/anyclaw-agents/AGENTS.md
autonomous: true
requirements: [JSON-04, CLON-01, CLON-03, D-09, D-10, BUGF-01, BUGF-02]

must_haves:
  truths:
    - "Zero serde_json::Value in agents manager.rs except D-03 boundaries (agent content mutation, JSON-RPC method params)"
    - "Clone count in agents manager.rs measurably reduced from 107 baseline"
    - "Borrowing used instead of ownership transfer where ownership isn't needed"
    - "Error enum audited — every fallible path uses typed errors"
    - "All grandfathered allows in agents lib.rs updated or removed"
  artifacts:
    - path: "crates/anyclaw-agents/src/manager.rs"
      provides: "Typed message handling, reduced clones, D-03 documented boundaries"
    - path: "crates/anyclaw-agents/src/lib.rs"
      provides: "Final allow annotations — only D-03 justified modules retain allow"
  key_links:
    - from: "crates/anyclaw-agents/src/manager.rs"
      to: "crates/anyclaw-agents/src/connection.rs"
      via: "IncomingAgentMessage now typed (from Plan 03)"
      pattern: "IncomingAgentMessage"
    - from: "crates/anyclaw-agents/src/manager.rs"
      to: "crates/anyclaw-sdk-types/src/acp.rs"
      via: "Typed ACP wire types from Phase 2"
      pattern: "SessionUpdateType|ContentPart"
---

<objective>
Type the agents manager.rs (3,781 lines) — the largest and most clone-heavy file in the workspace.

Purpose: Replace remaining serde_json::Value usages with typed alternatives, systematically reduce the 107-clone baseline, and audit error handling. This is the heaviest single file in the codebase. Plan 03 already typed the connection, slot, and platform_commands files — this plan builds on those typed foundations. Per D-04, full systematic audit of all clones. Per D-05, eliminate every clone that isn't strictly necessary.

Output: Typed agents manager with documented D-03 boundaries, measurably reduced clone count, audited error paths.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-manager-crate-quality/03-CONTEXT.md
@.planning/phases/03-manager-crate-quality/03-03-SUMMARY.md

<interfaces>
<!-- Plan 03 outputs (typed foundations) -->

From crates/anyclaw-agents/src/connection.rs (after Plan 03):
```rust
pub enum IncomingAgentMessage {
    AgentRequest(JsonRpcRequest),      // Was Value
    AgentNotification(JsonRpcRequest), // Was Value
}
// send_request() now returns oneshot::Receiver<JsonRpcResponse>
// send_raw() now accepts JsonRpcResponse
// pending_requests uses DashMap<u64, oneshot::Sender<JsonRpcResponse>>
```

From crates/anyclaw-agents/src/platform_commands.rs (after Plan 03):
```rust
pub struct PlatformCommand { name: String, description: String, ... }
pub fn platform_commands() -> Vec<PlatformCommand>;
pub fn platform_commands_value() -> serde_json::Value; // D-03 serialization boundary
```

From crates/anyclaw-agents/src/slot.rs (after Plan 03):
```rust
pub(crate) last_available_commands: Option<Vec<PlatformCommand>>, // or Option<Value> with D-03
```

<!-- Phase 2 typed foundations -->

From crates/anyclaw-sdk-types/src/acp.rs:
```rust
pub struct SessionUpdateEvent { pub session_id: String, pub session_update: SessionUpdateType, ... }
pub enum SessionUpdateType { Result, Message, Thought, ToolCall, ToolCallUpdate, ... }
pub struct ContentPart { pub r#type: String, pub text: Option<String>, ... }
```

From crates/anyclaw-jsonrpc/src/types.rs:
```rust
pub struct JsonRpcRequest { pub method: String, pub params: Option<Value>, pub id: Option<RequestId> }
pub struct JsonRpcResponse { pub result: Option<Value>, pub error: Option<JsonRpcError>, pub id: Option<RequestId> }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type all Value usages in agents manager.rs</name>
  <files>crates/anyclaw-agents/src/manager.rs</files>
  <read_first>
    - crates/anyclaw-agents/src/manager.rs (full file — 3,781 lines. Focus on the 24 Value usage sites listed below)
    - crates/anyclaw-agents/AGENTS.md (completion signal flow, tool event normalization, anti-patterns)
    - crates/anyclaw-agents/src/connection.rs (after Plan 03 — IncomingAgentMessage is now typed with JsonRpcRequest)
    - crates/anyclaw-agents/src/platform_commands.rs (after Plan 03 — typed PlatformCommand)
    - crates/anyclaw-agents/src/slot.rs (after Plan 03 — typed last_available_commands)
    - crates/anyclaw-sdk-types/src/acp.rs (SessionUpdateEvent, SessionUpdateType, ContentPart)
    - crates/anyclaw-sdk-types/src/channel_event.rs (ChannelEvent::DeliverMessage content field)
  </read_first>
  <action>
Type or document all 24 serde_json::Value usages in manager.rs. Group by function area:

**Struct fields (line 30):**
- `pub request: serde_json::Value` in PendingPermission struct — this stores the original JSON-RPC request from the agent so it can be forwarded back. Since Plan 03 typed IncomingAgentMessage to use JsonRpcRequest, change this to `pub request: JsonRpcRequest`. Update all sites that construct PendingPermission.

**Content mutation functions (lines 1133-1208):**
- `add_received_timestamp(content: &mut serde_json::Value)` — mutates raw JSON to add `_received_at_ms` timestamp. This is the core reason DeliverMessage.content stays as Value (Phase 2 decision). D-03 boundary — add comment: "D-03: agent content is arbitrary JSON that requires raw mutation (timestamps, normalization, command injection)".
- `inject_platform_commands(mut content: Value, ...)` — injects platform commands into availableCommands array. Same D-03 boundary. Add comment.
- `normalize_tool_event_fields(content: &mut Value, update_type: &str)` at line 1937 — normalizes agent-specific wire quirks. Same D-03 boundary. Add comment.
- `serde_json::Value::Array(platform_arr)` pattern match at line 1164 — part of inject_platform_commands. D-03.

**Session update handling (lines 1236-1400):**
- `handle_session_update(&mut self, slot_idx: usize, params: serde_json::Value)` — params come from JsonRpcRequest.params which is `Option<Value>` (D-03 method payload). Try to deserialize params into typed `SessionUpdateEvent` from sdk-types at the function entry: `let event: SessionUpdateEvent = serde_json::from_value(params)?`. Then use typed fields throughout the function body. If deserialization fails, log warning and return (don't crash).
- `handle_permission_request(request: &Value, params: &Value)` at line 1255 — request is now `&JsonRpcRequest` (from Plan 03 typed IncomingAgentMessage). params can be extracted from `request.params`. Update signature.
- `handle_fs_read(request: &Value, params: &Value)` at line 1340 — same pattern, update to `&JsonRpcRequest`.
- `handle_fs_write(request: &Value, params: &Value)` at line 1369 — same pattern.
- `send_result_to_agent(request: &Value, result: Value)` at line 1394 — request is now `&JsonRpcRequest`. Build a `JsonRpcResponse` struct using `request.id.clone()` instead of `request.get("id").cloned().unwrap_or(Value::Null)`. Write via connection.send_raw() which now accepts JsonRpcResponse (Plan 03).
- `send_error_to_agent(request: &Value, ...)` at line 1409 — same pattern, build typed JsonRpcResponse.

**Fallback defaults (lines 437, 894, 1087, 1633):**
- `request.get("id").cloned().unwrap_or(serde_json::Value::Null)` at lines 437, 1327, 1400, 1416 — these extract the request id. With typed JsonRpcRequest, use `request.id.clone()` directly. The id is `Option<RequestId>` — no more Value::Null fallback needed.
- `serde_json::Value::default()` at lines 894, 1633 — fallback for missing content. Replace with appropriate typed default (e.g., `ContentPart::default()` or empty string content).
- `.unwrap_or(serde_json::Value::Null)` at line 1087 — replace with typed default.

**Run loop message handling:**
- The run loop receives `IncomingAgentMessage` which is now typed (Plan 03). Update the match arms to destructure `JsonRpcRequest` fields directly instead of calling `.get("method")` on raw Value. Extract `method`, `params`, `id` from the typed struct.
  </action>
  <verify>
    <automated>cargo clippy -p anyclaw-agents -- -D warnings && cargo test -p anyclaw-agents</automated>
  </verify>
  <acceptance_criteria>
    - manager.rs does NOT contain `request.get("id").cloned().unwrap_or(serde_json::Value::Null)` (uses typed RequestId)
    - manager.rs does NOT contain `serde_json::Value::default()` (uses typed defaults)
    - handle_session_update deserializes params into SessionUpdateEvent at entry
    - handle_permission_request/handle_fs_read/handle_fs_write accept &JsonRpcRequest, not &Value
    - send_result_to_agent/send_error_to_agent build typed JsonRpcResponse
    - All remaining Value usages have D-03 justification comments (content mutation functions)
    - cargo clippy -p anyclaw-agents -- -D warnings exits 0
    - cargo test -p anyclaw-agents exits 0
  </acceptance_criteria>
  <done>All 24 Value usages in manager.rs typed or documented with D-03 justification. Message handling uses typed JsonRpcRequest/Response throughout.</done>
</task>

<task type="auto">
  <name>Task 2: Clone audit (107 baseline) + error audit + lib.rs allows + AGENTS.md</name>
  <files>crates/anyclaw-agents/src/manager.rs, crates/anyclaw-agents/src/lib.rs, crates/anyclaw-agents/AGENTS.md</files>
  <read_first>
    - crates/anyclaw-agents/src/manager.rs (after Task 1 — focus on .clone() calls, error handling paths)
    - crates/anyclaw-agents/src/error.rs (AgentsError enum)
    - crates/anyclaw-agents/src/acp_error.rs (AcpError enum)
    - crates/anyclaw-agents/src/lib.rs (current allows)
    - crates/anyclaw-agents/AGENTS.md (current documentation)
  </read_first>
  <action>
1. **Clone audit (D-04, D-05):** Systematically audit all ~107 .clone() calls in manager.rs. For each clone, categorize:
   - **Necessary — Arc:** `Arc::clone(&x)` for shared ownership across async tasks. Replace `.clone()` with explicit `Arc::clone()` for clarity.
   - **Necessary — channel send:** Values sent via mpsc/oneshot that require ownership. Keep but document if not obvious.
   - **Necessary — async move:** Values moved into async blocks/closures. Keep.
   - **Unnecessary — immediate use:** Value cloned then consumed immediately. Remove clone, use the value directly.
   - **Unnecessary — can borrow:** Value cloned but only read. Replace with `&str` or reference.
   - **Unnecessary — can move:** Value cloned but original not used after. Replace with move.

   Target: at least 30% reduction (from 107 to ~75 or fewer). Common patterns to fix:
   - `name.clone()` where `&name` or `name.as_str()` suffices
   - `session_key.clone()` where the key is only used for a lookup
   - `config.clone()` where a reference would work
   - String clones for logging (use `&str` or `tracing` field references)

2. **Error audit (D-09, D-10):**
   - Review AgentsError enum — confirm all variants are used, no dead variants
   - Check all `.map_err()` and `?` paths — ensure they use typed errors, not string conversion where a typed variant exists
   - Check for any bare `.unwrap()` in production code (not tests) — replace with `.expect("reason")` or `?`
   - Confirm no `anyhow` usage in this library crate

3. **Update lib.rs allows:**
   - `manager` module: update comment from "Grandfathered: typed replacement in Phase 2-4" to specific D-03 justification: "D-03: agent content mutation requires raw Value (timestamps, normalization, command injection)"
   - Verify all other module allows were updated in Plan 03 Task 2

4. **Update AGENTS.md:**
   - Update any documentation that references Value-based APIs (e.g., IncomingAgentMessage, send_raw, pending_requests)
   - Add note about DashMap migration
   - Add note about typed JsonRpcMessage pipeline
  </action>
  <verify>
    <automated>cargo clippy -p anyclaw-agents -- -D warnings && cargo test -p anyclaw-agents && cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - manager.rs clone count reduced by at least 30% from 107 baseline (run: `rg -c '\.clone\(\)' crates/anyclaw-agents/src/manager.rs` — must be ≤75)
    - All Arc clones use explicit `Arc::clone()` syntax
    - No bare `.unwrap()` in manager.rs production code (test code is fine)
    - No `anyhow` usage in anyclaw-agents crate
    - lib.rs manager module allow has D-03 justification comment
    - AGENTS.md updated to reflect typed APIs (DashMap, JsonRpcMessage pipeline)
    - cargo build --workspace exits 0
  </acceptance_criteria>
  <done>Clone count reduced ≥30%, error paths audited, lib.rs allows finalized, AGENTS.md updated. Phase 3 complete — all three manager crates typed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent→manager | Agent sends session updates, permission requests, fs requests via JSON-RPC |
| manager→channels | Manager forwards typed ChannelEvents to channels manager |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | T (Tampering) | manager.rs content mutation | accept | Content mutation (timestamps, tool normalization, command injection) operates on agent-trusted data — agents are admin-configured |
| T-03-08 | I (Info Disclosure) | manager.rs session data | accept | Session data flows between admin-configured agents and channels — no user-facing exposure |
</threat_model>

<verification>
```bash
cargo clippy -p anyclaw-agents -- -D warnings
cargo test -p anyclaw-agents
cargo build --workspace
```
</verification>

<success_criteria>
- Zero `serde_json::Value` in manager.rs except D-03 justified boundaries (agent content mutation, JSON-RPC method params)
- Clone count measurably reduced from 107 baseline (target: at least 30% reduction)
- Borrowing (`&str`, references) used instead of ownership transfer where ownership isn't needed
- All D-03 Value usages have justification comments
- Error enum audited — no new anyhow, all fallible paths use typed errors
- `#[allow(clippy::disallowed_types)]` on manager module in lib.rs updated with D-03 justification
- `cargo clippy -p anyclaw-agents -- -D warnings` passes
- `cargo test -p anyclaw-agents` passes
- `cargo build --workspace` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-manager-crate-quality/03-04-SUMMARY.md`
</output>
