---
phase: 03-manager-crate-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/anyclaw-tools/Cargo.toml
  - crates/anyclaw-tools/src/lib.rs
  - crates/anyclaw-tools/src/manager.rs
  - crates/anyclaw-tools/src/mcp_host.rs
  - crates/anyclaw-tools/src/external.rs
  - crates/anyclaw-tools/src/wasm_runner.rs
  - crates/anyclaw-tools/src/wasm_tool.rs
autonomous: true
requirements: [JSON-06, CLON-02, CLON-03, BUGF-01, BUGF-02]

must_haves:
  truths:
    - "Zero serde_json::Value in anyclaw-tools production code except D-03 Tool trait boundaries"
    - "All #[allow(clippy::disallowed_types)] removed from tools lib.rs except justified D-03 modules"
    - "Clone count in tools crate reduced — no unnecessary clones remain"
    - "dashmap added as workspace dependency for downstream plans"
  artifacts:
    - path: "crates/anyclaw-tools/src/lib.rs"
      provides: "Updated module allows — only D-03 justified modules retain allow"
    - path: "crates/anyclaw-tools/src/manager.rs"
      provides: "Typed tool call args, reduced clones"
    - path: "Cargo.toml"
      provides: "dashmap workspace dependency"
  key_links:
    - from: "crates/anyclaw-tools/src/manager.rs"
      to: "crates/anyclaw-sdk-tool/src/trait_def.rs"
      via: "Tool trait interface"
      pattern: "fn input_schema|fn execute"
---

<objective>
Type the anyclaw-tools crate and add dashmap workspace dependency.

Purpose: Replace all serde_json::Value usages in the tools manager crate with typed alternatives, reduce unnecessary clones, and add dashmap to workspace deps for Plans 02/03. The Tool trait in sdk-tool uses Value for input_schema/execute — these are D-03 extensible boundaries that stay as Value with documented justification.

Output: Zero Value in tools production code (except D-03 trait boundaries), reduced clones, dashmap in workspace Cargo.toml.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-manager-crate-quality/03-CONTEXT.md

<interfaces>
<!-- Phase 2 typed foundations this plan builds on -->

From crates/anyclaw-sdk-tool/src/trait_def.rs:
```rust
pub trait Tool: Send + Sync + 'static {
    fn name(&self) -> &str;
    fn description(&self) -> &str;
    fn input_schema(&self) -> serde_json::Value;  // D-03: stays Value
    fn execute(&self, input: serde_json::Value) -> Result<serde_json::Value, ToolSdkError>;  // D-03: stays Value
}
```

From crates/anyclaw-core/src/lib.rs:
```rust
pub enum ToolsCommand {
    GetMcpUrls { reply: oneshot::Sender<Vec<McpServerUrl>> },
    CallTool { name: String, args: Option<serde_json::Map<String, Value>>, reply: oneshot::Sender<Result<Value, String>> },
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dashmap workspace dep + type tools manager Value usages</name>
  <files>Cargo.toml, crates/anyclaw-tools/Cargo.toml, crates/anyclaw-tools/src/manager.rs, crates/anyclaw-tools/src/mcp_host.rs</files>
  <read_first>
    - Cargo.toml (workspace dependencies section)
    - crates/anyclaw-tools/Cargo.toml
    - crates/anyclaw-tools/src/manager.rs
    - crates/anyclaw-tools/src/mcp_host.rs
    - crates/anyclaw-sdk-tool/src/trait_def.rs (Tool trait — D-03 boundary)
    - crates/anyclaw-core/src/lib.rs (ToolsCommand definition)
  </read_first>
  <action>
1. Add `dashmap = "6"` to `[workspace.dependencies]` in root Cargo.toml (per D-08). Do NOT add it to anyclaw-tools yet — it's for Plans 02/03.

2. In `manager.rs`:
   - The `ToolsCommand::CallTool { args: Option<serde_json::Map<String, Value>> }` comes from anyclaw-core — this is a D-03 boundary (arbitrary tool args). The manager receives it and passes to Tool::execute. Document with D-03 comment where Value is used.
   - The `AggregatedToolServer` test mock at line ~448 uses `fn input_schema(&self) -> serde_json::Value` and `fn execute(&self, input: serde_json::Value)` — these implement the Tool trait, D-03 boundary. Add struct-level `#[allow(clippy::disallowed_types)]` with D-03 comment on the test mock struct.
   - Audit all 18 `.clone()` calls. The manager clones `Arc` handles, config strings, and tool names. Replace unnecessary clones: use `&str` references where ownership isn't needed, use `Arc::clone()` explicitly for Arc clones (clarity), remove clones where the value is consumed immediately after.

3. In `mcp_host.rs`:
   - `McpHost::call_tool(args: Option<serde_json::Map<String, Value>>)` — D-03 boundary (tool args are arbitrary JSON). Add D-03 comment.
   - `fn input_schema(&self) -> serde_json::Value` and `fn execute(&self, input: serde_json::Value)` on the Tool impl — D-03 boundary. Add struct-level allow with D-03 comment.
  </action>
  <verify>
    <automated>cargo clippy -p anyclaw-tools -- -D warnings && cargo test -p anyclaw-tools</automated>
  </verify>
  <acceptance_criteria>
    - root Cargo.toml contains `dashmap = "6"` in [workspace.dependencies]
    - manager.rs D-03 Value usages have justification comments
    - mcp_host.rs D-03 Value usages have justification comments
    - No unnecessary .clone() calls remain in manager.rs (Arc clones use Arc::clone() explicitly)
    - cargo clippy -p anyclaw-tools -- -D warnings exits 0
  </acceptance_criteria>
  <done>dashmap in workspace deps, manager.rs and mcp_host.rs Value usages documented as D-03, clone audit complete for manager</done>
</task>

<task type="auto">
  <name>Task 2: Type external.rs + wasm files, update lib.rs allows, clone audit</name>
  <files>crates/anyclaw-tools/src/external.rs, crates/anyclaw-tools/src/wasm_runner.rs, crates/anyclaw-tools/src/wasm_tool.rs, crates/anyclaw-tools/src/lib.rs</files>
  <read_first>
    - crates/anyclaw-tools/src/external.rs
    - crates/anyclaw-tools/src/wasm_runner.rs
    - crates/anyclaw-tools/src/wasm_tool.rs
    - crates/anyclaw-tools/src/lib.rs
  </read_first>
  <action>
1. In `external.rs`:
   - `serialize_option_value(value: &serde_json::Value) -> String` at line 16 converts config option values to env var strings. These config options come from `HashMap<String, serde_json::Value>` in McpServerConfig (anyclaw-config). This is a D-03 boundary — config options are arbitrary user-defined values. Add D-03 comment. The function itself is fine — it pattern-matches Value variants to strings.
   - Reduce clones: `serde_json::Value::String(s) => s.clone()` — check if `s.as_str().to_owned()` or returning `&str` is viable.

2. In `wasm_runner.rs`:
   - `options: &HashMap<String, serde_json::Value>` at lines 100, 120, 200 — these are WASM sandbox config options from WasmToolConfig. D-03 boundary (arbitrary user config). Add D-03 comment.
   - `serde_json::Value::String(s) => s.clone()` at line 223 — same pattern as external.rs. Reduce clone if possible.
   - Audit 7 clones: replace unnecessary ones with references or Arc::clone().

3. In `wasm_tool.rs`:
   - `fn input_schema(&self) -> serde_json::Value` and `fn execute(&self, input: Value) -> Result<Value, ToolSdkError>` — D-03 Tool trait boundary. Add struct-level allow with D-03 comment.
   - `serde_json::from_str(&output).unwrap_or(serde_json::Value::String(output))` at line 67 — this is the WASM output parsing fallback. The output is arbitrary tool output, D-03 boundary. Add comment.
   - Audit 1 clone in this file.

4. In `lib.rs`:
   - Update all 5 `#[allow(clippy::disallowed_types)]` annotations. For modules where ALL Value usages are now D-03 justified, update the comment from "Grandfathered: typed replacement in Phase 2-4" to "D-03: Tool trait boundary — input_schema/execute use Value for arbitrary tool schemas" (or similar specific justification per module).
   - If any module is now completely Value-free, REMOVE the allow entirely.
  </action>
  <verify>
    <automated>cargo clippy -p anyclaw-tools -- -D warnings && cargo test -p anyclaw-tools && cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - external.rs Value usages have D-03 justification comments
    - wasm_runner.rs Value usages have D-03 justification comments
    - wasm_tool.rs has struct-level allow with D-03 comment
    - lib.rs allow comments updated from "Grandfathered" to specific D-03 justifications
    - No unnecessary .clone() calls remain in external.rs, wasm_runner.rs, wasm_tool.rs
    - cargo build --workspace exits 0
  </acceptance_criteria>
  <done>All tools crate files typed/documented, lib.rs allows updated, clone audit complete across entire crate, workspace builds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WASM sandbox | Untrusted WASM modules execute with fuel limits and WASI isolation |
| External MCP servers | Subprocess binaries communicate via stdio JSON-RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T (Tampering) | wasm_runner.rs | accept | WASM sandbox already enforces fuel budgets and WASI isolation — typing doesn't change trust boundary |
| T-03-02 | I (Info Disclosure) | external.rs | accept | External MCP servers are admin-configured, not user-facing — env var serialization is config-only |
</threat_model>

<verification>
```bash
cargo clippy -p anyclaw-tools -- -D warnings
cargo test -p anyclaw-tools
cargo build --workspace
```
</verification>

<success_criteria>
- Zero `serde_json::Value` in tools production code except D-03 justified boundaries (Tool trait input_schema/execute, ToolsCommand::CallTool args)
- All grandfathered `#[allow(clippy::disallowed_types)]` in lib.rs updated — only D-03 justified modules retain allows
- `dashmap` present in `[workspace.dependencies]` in root Cargo.toml
- Clone count reduced in tools crate files
- `cargo clippy -p anyclaw-tools -- -D warnings` passes
- `cargo test -p anyclaw-tools` passes
- `cargo build --workspace` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-manager-crate-quality/03-01-SUMMARY.md`
</output>
