---
phase: 03-manager-crate-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/anyclaw-channels/Cargo.toml
  - crates/anyclaw-channels/src/lib.rs
  - crates/anyclaw-channels/src/connection.rs
  - crates/anyclaw-channels/src/manager.rs
  - crates/anyclaw-channels/src/debug_http.rs
autonomous: true
requirements: [JSON-05, ADVN-01, CLON-02, CLON-03, D-07, BUGF-01, BUGF-02]

must_haves:
  truths:
    - "Zero serde_json::Value in anyclaw-channels production code except D-03 boundaries"
    - "Arc<Mutex<HashMap>> in connection.rs replaced with DashMap"
    - "Clone count in channels crate reduced — no unnecessary clones remain"
    - "Codec boundary shims (from_value/to_value) in connection.rs replaced with typed JsonRpcMessage pipeline"
  artifacts:
    - path: "crates/anyclaw-channels/src/connection.rs"
      provides: "DashMap for pending_requests, typed JsonRpcMessage pipeline, no codec shims"
    - path: "crates/anyclaw-channels/src/manager.rs"
      provides: "Typed message content, reduced clones"
    - path: "crates/anyclaw-channels/src/lib.rs"
      provides: "Updated module allows — only D-03 justified modules retain allow"
  key_links:
    - from: "crates/anyclaw-channels/src/connection.rs"
      to: "crates/anyclaw-jsonrpc/src/codec.rs"
      via: "NdJsonCodec now produces JsonRpcMessage directly"
      pattern: "JsonRpcMessage"
    - from: "crates/anyclaw-channels/src/connection.rs"
      to: "dashmap::DashMap"
      via: "pending_requests field"
      pattern: "DashMap"
---

<objective>
Type the anyclaw-channels crate, migrate to DashMap, and remove codec boundary shims.

Purpose: Replace all serde_json::Value usages with typed alternatives, replace Arc<Mutex<HashMap>> with DashMap for lock-free concurrent access to pending requests, remove the Phase 2 codec boundary shims (from_value/to_value) so the channel connection consumes JsonRpcMessage directly from the codec, and reduce unnecessary clones. Per D-02, managers consume JsonRpcMessage directly — no intermediate Value conversion.

Output: Zero Value in channels production code (except D-03 boundaries), DashMap for pending_requests, typed codec pipeline, reduced clones.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-manager-crate-quality/03-CONTEXT.md

<interfaces>
<!-- Phase 2 typed foundations -->

From crates/anyclaw-jsonrpc/src/types.rs:
```rust
pub enum JsonRpcMessage {
    Request(JsonRpcRequest),
    Response(JsonRpcResponse),
}
pub struct JsonRpcRequest {
    pub jsonrpc: String,
    pub method: String,
    pub params: Option<serde_json::Value>,  // D-03
    pub id: Option<RequestId>,
}
pub struct JsonRpcResponse {
    pub jsonrpc: String,
    pub result: Option<serde_json::Value>,  // D-03
    pub error: Option<JsonRpcError>,
    pub id: Option<RequestId>,
}
pub enum RequestId { Number(u64), String(String) }
```

From crates/anyclaw-jsonrpc/src/codec.rs:
```rust
impl Decoder for NdJsonCodec {
    type Item = JsonRpcMessage;  // Typed — no longer Value
    type Error = JsonRpcCodecError;
}
impl Encoder<JsonRpcMessage> for NdJsonCodec { ... }
impl Encoder<JsonRpcRequest> for NdJsonCodec { ... }
impl Encoder<JsonRpcResponse> for NdJsonCodec { ... }
```

From crates/anyclaw-sdk-types/src/channel_event.rs:
```rust
pub enum ChannelEvent {
    DeliverMessage { session_key: SessionKey, content: serde_json::Value },  // D-03: agent content is arbitrary
    SessionComplete { session_key: SessionKey },
    RequestPermission { session_key: SessionKey, request: serde_json::Value },  // D-03
}
```

Current codec shims in channels/connection.rs (TO BE REMOVED):
```rust
// line 107: let Ok(typed_msg) = serde_json::from_value::<JsonRpcMessage>(msg)
// line 125: let value = serde_json::to_value(&typed_msg).unwrap_or_default();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DashMap migration + typed codec pipeline in connection.rs</name>
  <files>crates/anyclaw-channels/Cargo.toml, crates/anyclaw-channels/src/connection.rs</files>
  <read_first>
    - crates/anyclaw-channels/Cargo.toml
    - crates/anyclaw-channels/src/connection.rs (full file — 477 lines, understand codec shims and pending_requests)
    - crates/anyclaw-jsonrpc/src/types.rs (JsonRpcMessage, JsonRpcRequest, JsonRpcResponse, RequestId)
    - crates/anyclaw-jsonrpc/src/codec.rs (NdJsonCodec Decoder/Encoder impls)
    - crates/anyclaw-channels/src/error.rs (ChannelsError variants)
  </read_first>
  <action>
1. Add `dashmap = { workspace = true }` to `[dependencies]` in channels Cargo.toml. The workspace dep was added in Plan 01.

2. In `connection.rs`, replace `Arc<Mutex<HashMap<u64, oneshot::Sender<serde_json::Value>>>>` with `Arc<DashMap<u64, oneshot::Sender<JsonRpcResponse>>>`:
   - Change the `pending_requests` field type in `ChannelConnection` struct (line ~39)
   - Change the `pending_requests` parameter in `spawn()` (line ~94) and `send_request()` (line ~191)
   - Replace `pending_requests.lock().await.insert(id, tx)` with `pending_requests.insert(id, tx)`
   - Replace `pending_requests.lock().await.remove(&id)` with `pending_requests.remove(&id).map(|(_, v)| v)`
   - The oneshot sender now sends `JsonRpcResponse` instead of `Value`

3. Remove codec boundary shims:
   - Line ~107: `serde_json::from_value::<JsonRpcMessage>(msg)` — the codec already produces `JsonRpcMessage` directly. Remove the from_value conversion. The reader loop should match on `JsonRpcMessage::Request` and `JsonRpcMessage::Response` directly.
   - Line ~125: `serde_json::to_value(&typed_msg).unwrap_or_default()` — the codec accepts `JsonRpcMessage` (and `JsonRpcRequest`/`JsonRpcResponse` via convenience Encoder impls). Remove the to_value conversion. Write `JsonRpcRequest` or `JsonRpcResponse` directly to the framed writer.

4. Update `IncomingChannelMessage` enum:
   - `ChannelRequest(serde_json::Value)` → `ChannelRequest(JsonRpcRequest)` — the request is now typed
   - `ChannelNotification(serde_json::Value)` → `ChannelNotification(JsonRpcRequest)` — notifications are requests without id
   - Update the reader task to construct these from the typed `JsonRpcMessage` variants

5. Update `send_request()` (line ~191):
   - `params: serde_json::Value` → `params: serde_json::Value` — params stay as Value (D-03: method-specific payloads). But the function should build a `JsonRpcRequest` struct and write it directly to the framed writer instead of building a `serde_json::json!({...})` Value.
   - The response receiver now returns `JsonRpcResponse` instead of `Value`.

6. Update `send_notification()` (line ~217):
   - Same pattern — build `JsonRpcRequest` (with `id: None`) and write directly.

7. Audit 4 clones in connection.rs — replace unnecessary ones with references or Arc::clone().
  </action>
  <verify>
    <automated>cargo clippy -p anyclaw-channels -- -D warnings && cargo test -p anyclaw-channels</automated>
  </verify>
  <acceptance_criteria>
    - connection.rs contains `DashMap` import and uses it for pending_requests
    - connection.rs does NOT contain `serde_json::from_value` or `serde_json::to_value`
    - connection.rs does NOT contain `Arc<Mutex<HashMap`
    - IncomingChannelMessage variants use JsonRpcRequest instead of Value
    - send_request builds JsonRpcRequest struct, not json!({}) Value
    - cargo clippy -p anyclaw-channels -- -D warnings exits 0
  </acceptance_criteria>
  <done>DashMap replaces Arc<Mutex<HashMap>>, codec shims removed, connection.rs uses typed JsonRpcMessage pipeline</done>
</task>

<task type="auto">
  <name>Task 2: Type manager.rs + debug_http.rs, update lib.rs allows, clone audit</name>
  <files>crates/anyclaw-channels/src/manager.rs, crates/anyclaw-channels/src/debug_http.rs, crates/anyclaw-channels/src/lib.rs</files>
  <read_first>
    - crates/anyclaw-channels/src/manager.rs (1,546 lines — focus on Value usages at lines 26, 357, 443, 561, 570, 599, 654, 816, 871)
    - crates/anyclaw-channels/src/debug_http.rs (focus on Value usages at lines 77, 192, 227, 257)
    - crates/anyclaw-channels/src/lib.rs
    - crates/anyclaw-channels/src/connection.rs (after Task 1 — IncomingChannelMessage is now typed)
    - crates/anyclaw-sdk-types/src/channel_event.rs (ChannelEvent — D-03 boundary)
  </read_first>
  <action>
1. In `manager.rs`:
   - Line 26: `content: serde_json::Value` in a struct field — this is agent content forwarded to channels. It comes from `ChannelEvent::DeliverMessage { content: Value }` which is D-03 (agent content is arbitrary JSON that the agents manager mutates). Add D-03 comment.
   - Line 357: same `content: serde_json::Value` in a method param — D-03, add comment.
   - Line 443: `serde_json::Value::Array(vec![])` — empty array for available commands. Type this as an actual empty Vec or use the typed AvailableCommand if one exists. If it's building JSON-RPC params, build a typed struct instead.
   - Line 561: `parse_channel_message` returns `(String, serde_json::Value)` — the Value is the parsed params from IncomingChannelMessage. Since Task 1 typed IncomingChannelMessage to use JsonRpcRequest, update this to extract params from the typed request. Params stay as `Option<Value>` (D-03 method payloads).
   - Line 570: `.unwrap_or(serde_json::Value::Null)` — update to work with typed params.
   - Line 599: `params: serde_json::Value` in `send_to_channel` — this builds a JSON-RPC request to send to the channel subprocess. Use `JsonRpcRequest` struct directly instead of raw Value.
   - Line 654: `"messageId": serde_json::Value::Null` — part of a json!({}) block building channel/deliverMessage params. Type this as a struct or at minimum document as D-03 (channel protocol params are method-specific).
   - Line 816: `serde_json::Value::default()` — fallback for missing content. Replace with appropriate typed default.
   - Line 871: `handle_respond_permission(&self, params: serde_json::Value, ...)` — permission response params. D-03 boundary (protocol-specific). Add comment.
   - Audit all 31 clones: categorize as necessary (Arc, async move, channel send) vs unnecessary. Replace unnecessary clones with references, Arc::clone(), or ownership transfer.

2. In `debug_http.rs`:
   - Line 77: `Json<serde_json::Value>` return type for health endpoint — replace with a typed `HealthResponse` struct with `#[derive(Serialize)]`.
   - Lines 192, 227, 257: `serde_json::Value` in test assertions — these are test code, bare Value is acceptable in tests. Add `#[allow(clippy::disallowed_types)]` at test module level if needed.

3. In `lib.rs`:
   - Update all 3 `#[allow(clippy::disallowed_types)]` annotations:
     - `connection` module: if now Value-free (after DashMap + typed pipeline), REMOVE the allow entirely
     - `manager` module: update comment to "D-03: ChannelEvent content and JSON-RPC method params are arbitrary agent/protocol JSON"
     - `debug_http` module: if only test code uses Value, REMOVE the allow (test modules have their own allows) or update comment
  </action>
  <verify>
    <automated>cargo clippy -p anyclaw-channels -- -D warnings && cargo test -p anyclaw-channels && cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - manager.rs Value usages have D-03 justification comments where they remain
    - manager.rs does NOT contain json!({}) blocks for building JSON-RPC messages (uses typed structs)
    - debug_http.rs health endpoint returns typed HealthResponse, not Json<Value>
    - lib.rs allow comments updated from "Grandfathered" to specific D-03 justifications or removed
    - Clone count in manager.rs measurably reduced from 31 baseline
    - cargo build --workspace exits 0
  </acceptance_criteria>
  <done>Channels crate fully typed, lib.rs allows updated, clone audit complete, DashMap integrated, workspace builds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| channel subprocess → manager | Inbound user messages cross trust boundary via JSON-RPC |
| manager → channel subprocess | Outbound agent content forwarded to channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | T (Tampering) | connection.rs | accept | Channel subprocesses are admin-configured binaries, not user-controlled — same trust as any configured service |
| T-03-04 | D (DoS) | connection.rs DashMap | accept | DashMap is lock-free but still bounded by channel count (small, admin-configured) — no amplification risk |
</threat_model>

<verification>
```bash
cargo clippy -p anyclaw-channels -- -D warnings
cargo test -p anyclaw-channels
cargo build --workspace
```
</verification>

<success_criteria>
- Zero `serde_json::Value` in channels production code except D-03 justified boundaries
- `Arc<Mutex<HashMap<u64, oneshot::Sender<Value>>>>` replaced with `DashMap<u64, oneshot::Sender<JsonRpcResponse>>` in connection.rs
- Codec boundary shims (`serde_json::from_value`, `serde_json::to_value`) removed from connection.rs
- Connection reads/writes `JsonRpcMessage` directly via the typed codec
- All grandfathered `#[allow(clippy::disallowed_types)]` in lib.rs updated with D-03 justifications or removed
- Clone count reduced in manager.rs (31 baseline)
- `cargo clippy -p anyclaw-channels -- -D warnings` passes
- `cargo test -p anyclaw-channels` passes
- `cargo build --workspace` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-manager-crate-quality/03-02-SUMMARY.md`
</output>
