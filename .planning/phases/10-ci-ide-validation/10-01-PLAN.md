---
phase: 10-ci-ide-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/anyclaw/src/init.rs
autonomous: true
requirements:
  - IDE-01
  - CI-01
  - CI-02
must_haves:
  truths:
    - "anyclaw init output starts with a yaml-language-server modeline comment"
    - "The modeline references anyclaw.schema.json with a relative path"
    - "The generated YAML still deserializes to a valid AnyclawConfig"
    - "CI-01 and CI-02 are already satisfied by Phase 9 tests (no changes needed)"
  artifacts:
    - path: "crates/anyclaw/src/init.rs"
      provides: "generate_config_yaml with modeline"
      contains: "yaml-language-server"
  key_links:
    - from: "crates/anyclaw/src/init.rs"
      to: "anyclaw.schema.json"
      via: "modeline comment referencing schema file"
      pattern: "yaml-language-server.*schema.*anyclaw\\.schema\\.json"
---

<objective>
Add YAML modeline to `anyclaw init` output so IDEs with yaml-language-server automatically associate the config with the JSON Schema.

Purpose: IDE-01 — users get schema-powered autocomplete and validation in their editor immediately after `anyclaw init`.
Output: Updated `generate_config_yaml()` with modeline as first line.

Note: CI-01 and CI-02 are already satisfied by Phase 9's drift test (`when_committed_schema_file_exists_then_it_matches_generate_schema_output`) and example validation test (`when_example_configs_loaded_then_they_validate_against_schema`). Both run in CI via `cargo nextest run`. No CI workflow changes needed (per D-01, D-02, D-03).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/anyclaw/src/init.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add YAML modeline to generate_config_yaml</name>
  <files>crates/anyclaw/src/init.rs</files>
  <read_first>
    - crates/anyclaw/src/init.rs (current generate_config_yaml implementation and existing tests)
    - anyclaw.schema.json (confirm file exists at repo root)
  </read_first>
  <behavior>
    - Test: `when_generate_config_yaml_called_then_first_line_is_yaml_language_server_modeline` — first line of output equals `# yaml-language-server: $schema=./anyclaw.schema.json`
    - Test: `when_generate_config_yaml_called_then_yaml_with_modeline_deserializes_to_valid_config` — the full output (including modeline comment) still parses as valid AnyclawConfig via serde_yaml
  </behavior>
  <action>
Per D-04 and D-05:

1. Add two new tests to `mod tests` in `init.rs` using rstest conventions (no `test_` prefix, BDD naming):
   - `when_generate_config_yaml_called_then_first_line_is_yaml_language_server_modeline`: call `generate_config_yaml("opencode")`, assert `output.lines().next().unwrap() == "# yaml-language-server: $schema=./anyclaw.schema.json"`
   - `when_generate_config_yaml_called_then_yaml_with_modeline_deserializes_to_valid_config`: call `generate_config_yaml("opencode")`, assert `serde_yaml::from_str::<anyclaw_config::AnyclawConfig>(&output).is_ok()`

2. Run tests — both new tests MUST fail (RED).

3. Update `generate_config_yaml()` format string: prepend `# yaml-language-server: $schema=./anyclaw.schema.json\n` as the very first line, before the existing `# Anyclaw configuration` comment. The relative path `./anyclaw.schema.json` assumes the schema file is in the same directory as `anyclaw.yaml` (repo root convention).

4. Run tests — all tests MUST pass (GREEN), including existing tests:
   - `when_generate_config_yaml_called_then_contains_agent_binary`
   - `when_generate_config_yaml_called_then_yaml_contains_comment_lines`
   - `when_generate_config_yaml_called_then_contains_agents_and_channels_but_not_supervisor`
   - `when_generate_config_yaml_called_then_yaml_deserializes_to_valid_anyclaw_config`
   - `given_no_existing_config_when_run_init_then_creates_file_with_valid_yaml`
  </action>
  <verify>
    <automated>cargo test -p anyclaw -- init::tests --no-fail-fast</automated>
  </verify>
  <acceptance_criteria>
    - `init.rs` contains the string `yaml-language-server: $schema=./anyclaw.schema.json`
    - `generate_config_yaml("opencode").lines().next()` equals `# yaml-language-server: $schema=./anyclaw.schema.json`
    - `cargo test -p anyclaw -- init::tests` exits 0 with all tests passing
  </acceptance_criteria>
  <done>generate_config_yaml() output starts with yaml-language-server modeline; all init tests pass including new modeline-specific tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This task only modifies a static format string in init output — no user input processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T (Tampering) | init.rs modeline path | accept | Modeline is a comment — yaml-language-server treats it as a hint, not executable. Relative path `./` is standard convention. No security impact. |
</threat_model>

<verification>
- `cargo test -p anyclaw -- init::tests` — all pass
- `cargo clippy -p anyclaw` — no warnings
- Manual: `grep "yaml-language-server" crates/anyclaw/src/init.rs` returns the modeline line
</verification>

<success_criteria>
`anyclaw init` output starts with `# yaml-language-server: $schema=./anyclaw.schema.json` and all existing + new tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ci-ide-validation/10-01-SUMMARY.md`
</output>
